# 1. Connect — Source-universe and custody successor requirements v2 Producer QA

## 1.1 Identity and limits

1.1.1 `artifactId=CONNECT-SOURCE-UNIVERSE-AND-CUSTODY-SUCCESSOR-REQUIREMENTS-V2-PRODUCER-QA-2026-08-29`.

1.1.2 subject path=`/Users/tal/Documents/connect/web/docs/planning/source-universe-and-custody-successor-requirements-v2-2026-08-29.md`.

1.1.3 subject raw SHA-256=`5eb769c734690f4cde2885c798c3275d711791b9c1ea6d1a71d0badd0dea25fe`.

1.1.4 physical identity=`437 lines/36330 bytes/UTF-8`.

1.1.5 QA role=`Producer mechanical and coverage inspection`; independence=`NO`; Acceptance authority=`NONE`.

1.1.6 this QA performs no source custody, extraction, network retrieval, account inspection, Product/Git/Push/Deploy/provider mutation or Gate action.

# 2. Mechanical results

## 2.1 Requirement and graph shape

2.1.1 Requirement headings=`26`; unique IDs=`26`; exact sequence=`SURS-001` through `SURS-026`; gaps=`0`; duplicates=`0`.

2.1.2 each field count is exact: `rule=26`, `causeAndEffect=26`, `sourceIds=26`, `acceptancePredicate=26`, `dependencies=26`.

2.1.3 numbered-clause duplicates=`0`.

2.1.4 dependency edges=`74`; unknown targets=`0`; self edges=`0`; duplicate edges per Requirement=`0`; detected cycles=`0`.

## 2.2 Review-source coverage

2.2.1 reviewer-local identities represented literally=`32/32`; exact set=`SURS-HR-F001` through `SURS-HR-F032`; shorthand ranges and suffix-only IDs=`0`.

2.2.2 Section 7 provides one forward mapping, hostile vector and safe terminal for every reviewer-local Finding.

2.2.3 direct identity coverage grants zero closure; independent reviewers must prove `32/32 FULL` semantic sufficiency against this exact subject root.

# 3. Producer semantic inspection

## 3.1 Material changes from SURS-1

3.1.1 discovery begins from a finite frozen seed/policy/cutoff set and cannot define completeness circularly.

3.1.2 every source reference must resolve to an exact rooted claim with inverse readback.

3.1.3 authority, role, subject, interval, precedence and conflict are closed typed domains with fail-closed incomparable cases.

3.1.4 Git identity covers repository/worktree/index/overlay/mode/symlink/submodule/LFS/sparse/Unicode/case and separate remote observations.

3.1.5 discovery-family and exclusion classification include Unknown quarantine, provenance, license and symlink rules.

3.1.6 Candidate and Admitted records use canonical machine schemas and four orthogonal state machines.

3.1.7 raw custody, private derivatives and Public derivatives have separate identities, provenance and append-only custody events.

3.1.8 Public safety covers every declared egress surface before any Public-system write.

3.1.9 ingestion is an isolated bounded no-network-by-default state machine with hostile-file and SSRF controls.

3.1.10 byte fidelity is explicitly separated from authenticity, authority, freshness and sufficiency.

3.1.11 locator profiles cover text, PDF, code, database, web, DOCX, raster/OCR, audio/video, email/chat, API/JSON and archives under independent hermetic resolvers.

3.1.12 implementation discovery covers mixed languages/loaders and framework/dynamic/generated entries, not Regex or TypeScript alone.

3.1.13 implementation snapshot binds Git, overlay, remote observation, lockfiles, package manager, runtime and toolchain.

3.1.14 dynamic source capture includes publisher authenticity, canonical fetch profile, trusted time and conservative freshness/failure invalidation.

3.1.15 private Provider receipts and Public-safe projections are subject-bound, expiring, revocable and dependent on safety and lifecycle rules.

3.1.16 authority approvals use rooted role/Appointment/delegation/quorum/SoD/revocation semantics.

3.1.17 Decisions use append-only authenticated field-level events and a deterministic reducer.

3.1.18 D31 uses a portable source ID, exact span and blocking Unknown/conflict states rather than path presence or forced binary disposition.

3.1.19 selection events reduce the four state axes idempotently and block only their exact dependency closure.

3.1.20 denominators use one rooted CandidateSet and member-level set equations.

3.1.21 SourceSet has a canonical byte grammar plus complete typed forward/inverse relation closure.

3.1.22 invalidation uses a typed graph, complete trigger registry, least-fixed-point affected Set and atomic successor transaction.

3.1.23 archive/replay is DataClass-specific and binds retention, Legal Hold, keys, access, region, license and Backup/Restore Evidence.

3.1.24 Acceptance requires independent envelopes, finite rooted mutation oracles and zero open P0/P1/P2; `REVIEW_BLOCKED` is not Acceptance.

## 3.2 Residual limits

3.2.1 the document defines requirements only; no actual schemas, SourceReferenceIndex, SourceSet, discovery outputs, corpus, algorithms, custody receipts or Acceptance exist.

3.2.2 the subject has not yet received independent structural, security, mathematical or media/ingestion review.

3.2.3 all 32 predecessor Findings remain open until a root-bound independent closure process decides them; Producer QA cannot close them.

3.2.4 exact source, Requirement, Task, hour, completion and ETA denominators remain `unknown/unavailable`.

# 4. Disposition

## 4.1 Result and next safe action

4.1.1 mechanical result=`PASS-ON-RECORDED-CHECKS`.

4.1.2 Producer semantic result=`CORRECTION-INTENT-MATERIALIZED; NOT-INDEPENDENTLY-PROVED`.

4.1.3 accepted requirements=`0/26`; accepted reviewer Finding mappings=`0/32`.

4.1.4 next safe action=`freeze this exact subject root and run independent hostile reviews; create another successor for any remaining blocker; do not discover, ingest or admit sources under an unaccepted schema`.

4.1.5 `Gate29=BLOCKED`; `development freeze=ACTIVE`.
