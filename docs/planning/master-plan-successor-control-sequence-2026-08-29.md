# 1. Connect — Master Plan successor control sequence

## 1.1 Identity and claim limit

1.1.1 `artifactId=CONNECT-MASTER-PLAN-SUCCESSOR-CONTROL-SEQUENCE-2026-08-29`.

1.1.2 `sequenceVersion=MPSC-1.0-draft`.

1.1.3 status=`PLANNING-CONTROL-MAP; NOT-A-PROGRAM-TASK-REGISTRY; NOT-ACCEPTED`.

1.1.4 purpose=`show the finite control path from rejected Master to an exact-root Master that can later authorize Program planning`.

1.1.5 these are Planning-generation phases, not Product Tasks. They carry no Product hours, ETA, completion credit or implementation authority.

1.1.6 exact Product percentage, remaining person-hours, critical path duration and calendar ETA remain `unknown/unavailable`.

## 1.2 Core flow

1.2.1 `Recovery → raw audits → Bootstrap lifecycle → review Protocol → audit reconciliation → TRD-2 → Source universe → Requirement/Decision universe → Program leaves → QA/Reviews → Master root → Tal exact-root approval → Gate29 → new implementation instruction`.

1.2.2 any byte-changing correction creates a successor and returns to the relevant QA/Review phase; it never patches an accepted/reviewed root in place.

1.2.3 Public-repository hardening is mandatory before any future Push, but Public visibility itself remains the binding Decision and is not changed to Private.

# 2. Twenty-five ordered control phases

## 2.1 `PG00` — Recovery and observed baseline

2.1.1 objective=`freeze the durable Master, exact source roots, Git roots and loss of temporary evidence`.

2.1.2 outputs=`Recovery ledger; G0 observed baseline; source/PDF and directive receipts`.

2.1.3 exit=`two independent baseline reproductions plus accepted successor baseline root`.

2.1.4 current=`producer evidence materialized; successor/independent acceptance pending`.

## 2.2 `PG01` — Three raw Master audits

2.2.1 objective=`independently discover structural, security-semantic and schedule-estimate defects against one exact Master root`.

2.2.2 outputs=`3 reports + 3 local manifests; 73 reviewer-local observations`.

2.2.3 exit=`raw-byte preservation and exact local identity/cardinality verified`.

2.2.4 current=`raw preservation complete; formal normalization/reconciliation blocked`.

## 2.3 `PG02` — Bootstrap lifecycle v2

2.3.1 objective=`define a finite non-self-authorizing lifecycle for Candidate, QA, Reviews, approval, CAS, invalidation and replay`.

2.3.2 prerequisites=`55 BCA2 successor requirements; exact source freeze`.

2.3.3 outputs=`BCA2 Candidate; conformance corpus; two-generation proof; detached Acceptance envelope`.

2.3.4 exit=`55/55 requirements proven by exact-root independent reviews and external acceptance`.

2.3.5 current=`requirements materialized; Candidate and acceptance absent`.

## 2.4 `PG03` — Three-review Protocol v1.1

2.4.1 objective=`define lossless local Finding preservation, deterministic semantic identity, dual normalization, comparison, conflict and reconciliation`.

2.4.2 prerequisites=`PG02 accepted; 12 Intake defects; 35 MPRR requirements`.

2.4.3 outputs=`Protocol Candidate; schemas; vectors; Reviews A/B; protected Acceptance`.

2.4.4 exit=`35/35 requirements and two complete generations pass`.

2.4.5 current=`requirements Candidate materialized; hostile review pending`.

## 2.5 `PG04` — Formal reconciliation of the three Master audits

2.5.1 objective=`normalize and compare all 73 local observations without dropping or inventing one`.

2.5.2 prerequisites=`PG03 accepted; 3 eligible 18/18 envelopes; frozen source set; two independent normalizers`.

2.5.3 outputs=`lossless local manifests; parity report; identity registry; Comparison; Conflict/Resolution; Reconciliation manifest`.

2.5.4 exit=`local inverse coverage 73/73; orphan=0; unexplained downgrade/predicate loss=0; all blocking conflict explicit`.

2.5.5 current=`blocked; eligible envelopes=0/3; semantic digests=0/73`.

## 2.6 `PG05` — Source-universe and custody Definition

2.6.1 objective=`define how all possible sources are discovered, classified, protected, selected, rooted and invalidated`.

2.6.2 prerequisites=`PG02 accepted; Public-repository boundary; source-precedence ledger`.

2.6.3 outputs=`Source-universe Definition; schemas; discovery/selection/custody/replay vectors`.

2.6.4 exit=`26/26 SURS requirements pass exact-root reviews and acceptance`.

2.6.5 current=`requirements Candidate materialized; review/Definition absent`.

## 2.7 `PG06` — TRD-2 Requirement-manifest successor

2.7.1 objective=`replace the rejected 64-row authoring root with total member-level coverage of all audit, BCA2, SREQ, MATH and source requirements`.

2.7.2 prerequisites=`current structural/mathematical/security hostile reviews complete; Producer QA 7/7 preserved; PG04 reconciliation available where required`.

2.7.3 outputs=`successor requirement manifest; bidirectional source coverage; review snapshot`.

2.7.4 exit=`dangling=0; missing source identity=0; ranges in machine fields=0; every admitted input has explicit disposition`.

2.7.5 current=`first 64-row snapshot rejected by Producer QA and mathematical Review; remaining hostile reviews in progress`.

## 2.8 `PG07` — TRD-2 Definition

2.8.1 objective=`define the total machine contract for Work, Task, Output, Test, Evidence, graph, Gate, time, security, data lifecycle and acceptance`.

2.8.2 prerequisites=`PG02, PG03 and PG06 accepted`.

2.8.3 outputs=`schemas; canonical constructors; parser/graph implementations; vector corpus; two-generation proof; Definition Acceptance`.

2.8.4 exit=`all accepted Definition requirements proven; every P0/P1/P2 resolved or blocking; protected current-root CAS succeeds`.

2.8.5 current=`not started; no Materialization permit`.

## 2.9 `PG08` — Source Candidate inventory and custody

2.9.1 objective=`discover actual candidate sources across attachments, 91+ docs, code/config/schema/tests/workflows and official dynamic authorities`.

2.9.2 prerequisites=`PG05 and PG07 accepted; authorized custody and Public-safe handling`.

2.9.3 outputs=`candidate inventory; custody receipts; media locators; exclusions/quarantine; class denominators`.

2.9.4 exit=`all discovery families accounted; prohibited Public content=0; two-run membership parity`.

2.9.5 current=`blocked by Definitions and authority`.

## 2.10 `PG09` — Admitted SourceSet

2.10.1 objective=`select exact authoritative/evidence/context sources with precedence, claim limits and safe states`.

2.10.2 prerequisites=`PG08 complete; named selector/review authorities`.

2.10.3 outputs=`immutable SourceSet; selection assertions; conflicts; independent readbacks`.

2.10.4 exit=`candidate disposition coverage=100%; dangling/self-member/duplicate=0; root parity`.

2.10.5 current=`pending`.

## 2.11 `PG10` — Requirement, Question and Decision universe

2.11.1 objective=`atomize/classify all admitted statements; materialize Q001–Q083, critical prompts, D01–D31/amendments, conflicts, Requirements and applicability`.

2.11.2 prerequisites=`PG07 and PG09 accepted; corrected SREQ successor accepted`.

2.11.3 outputs=`Source/Span/Statement/Question/Decision/Conflict/Requirement/Applicability manifests and forward/inverse graph`.

2.11.4 exit=`all semantic spans classified; 83/83 Questions; Decision-source coverage complete; orphan=0; exact locators; no silent Question/Example promotion`.

2.11.5 current=`pending; Product Requirement denominator unknown`.

## 2.12 `PG11` — Scope, Stage and Package registry

2.12.1 objective=`resolve Pilot/Post-Pilot, Scope1/3/4, S00–S28, historical 21 packages, conditional capabilities and route/surface identities`.

2.12.2 prerequisites=`PG10 accepted`.

2.12.3 outputs=`Scope manifests; Package templates/instances; activation Decisions; mapping registry`.

2.12.4 exit=`overlap/alias ambiguity=0; inactive packages receive zero denominator/credit; every Requirement has one scope disposition`.

2.12.5 current=`pending`.

## 2.13 `PG12` — Planning-generation execution plan

2.13.1 objective=`define the exact deterministic acts that may materialize Program records from accepted inputs`.

2.13.2 prerequisites=`PG07, PG10 and PG11 accepted`.

2.13.3 outputs=`Planning-generation Candidate; tool/schema roots; resource/role appointments; permit scope; ordered run ledger`.

2.13.4 exit=`detached reviews/acceptance and one valid non-replayable Materialization permit`.

2.13.5 current=`pending; no Program identity authorized`.

## 2.14 `PG13` — Program Task-leaf materialization

2.14.1 objective=`materialize every necessary S00–S28/Scope/Package leaf with all mandatory local fields, no inheritance and no implementation credit`.

2.14.2 prerequisites=`PG12 accepted permit; accepted source/requirement/scope roots`.

2.14.3 outputs=`Task, Output, Test, Evidence, Edge, Join, Gate, Resource, Mutex and ExternalWait records`.

2.14.4 exit=`every leaf atomic and bounded; every producer unique; no parent hours/credit; all records schema-valid`.

2.14.5 current=`pending; Program Task denominator does not exist`.

## 2.15 `PG14` — Coverage and cyber crosswalks

2.15.1 objective=`map every Requirement, Decision, Finding, Threat, Control, Framework, Dynamic source, DataClass, Gate and conditional capability to executable leaves/tests/evidence`.

2.15.2 prerequisites=`PG13 frozen Candidate`.

2.15.3 outputs=`forward/inverse authority graph; framework/control/threat/data-lifecycle crosswalks; orphan reports`.

2.15.4 exit=`forward coverage=100% by active scope; inverse orphan=0; unsupported readiness claim=0`.

2.15.5 current=`pending`.

## 2.16 `PG15` — Resource-constrained estimates and schedules

2.16.1 objective=`assign observed resources/calendars/mutexes/waits and derive Actual, ETC, low/high feasible schedules and critical/near-critical paths`.

2.16.2 prerequisites=`PG13–PG14; every in-scope leaf estimated or typed Unknown; capacity and wait bounds observed`.

2.16.3 outputs=`five denominator vectors; unique-work totals; assignment/capacity manifests; low/high schedule snapshots`.

2.16.4 exit=`MATH-001–MATH-032 pass; no double count; all critical nodes bounded for any published ETA`.

2.16.5 current=`pending; percent/hours/ETA remain unknown`.

## 2.17 `PG16` — Producer QA and mutation

2.17.1 objective=`prove schema, reference, identity, atomicity, graph, coverage, schedule, authority, public-content and security invariants on the Program Candidate`.

2.17.2 prerequisites=`PG13–PG15 frozen roots`.

2.17.3 outputs=`two parser reports; two graph reports; conformance/mutation/attack results; Producer Finding manifest`.

2.17.4 exit=`parser/graph parity; dangling/duplicate/cycle/forbidden mutation=0; every surviving defect blocks`.

2.17.5 current=`pending`.

## 2.18 `PG17` — Independent Reviews A/B

2.18.1 objective=`obtain independent structural/security/semantic/schedule review of identical Program Candidate and Evidence bytes`.

2.18.2 prerequisites=`PG16 pass; Review B packet presealed before Review A disclosure; eligible Appointments`.

2.18.3 outputs=`Review A; Review B; local manifests; exact envelopes`.

2.18.4 exit=`all review roots valid; independence proven; full coverage recorded; no in-place subject change`.

2.18.5 current=`pending`.

## 2.19 `PG18` — Program Finding reconciliation and successor loop

2.19.1 objective=`compare all reviewer observations losslessly, resolve conflicts and create successor Candidate for every required correction`.

2.19.2 prerequisites=`PG03 accepted; PG17 complete`.

2.19.3 outputs=`Comparison/Reconciliation; Veto/Risk records; successor roots; repeated QA/Reviews as needed`.

2.19.4 exit=`all P0/P1/P2 closed with exact successor Evidence or explicitly blocking; unexplained downgrade/predicate loss=0`.

2.19.5 current=`pending`.

## 2.20 `PG19` — Public-repository hardening readiness

2.20.1 objective=`prepare exact authorized changes for branch/ruleset, review/status checks, Actions permissions/SHA pinning, secret/code/dependency scanning, disclosure, License/NOTICE and root reconciliation`.

2.20.2 prerequisites=`Public remains selected; exact canonical repo/diff; named Security/Repo authority; no prohibited content`.

2.20.3 outputs=`hardening change Candidate; scans; exact-diff permit request; remote readback procedure`.

2.20.4 exit=`planning evidence complete; actual GitHub mutation occurs only under a separate exact permit and readback`.

2.20.5 current=`planning gaps known; no mutation authorized; next Push blocked`.

## 2.21 `PG20` — Master assembly

2.21.1 objective=`assemble one immutable Master Candidate from accepted source, requirement, Program, crosswalk, schedule, decision and gate roots`.

2.21.2 prerequisites=`PG10–PG19 exact accepted/current roots`.

2.21.3 outputs=`Master machine root; numbered human view; navigation indexes; artifact inventory`.

2.21.4 exit=`human view regenerates from machine records; all referenced roots resolve; no circular/self membership`.

2.21.5 current=`pending`.

## 2.22 `PG21` — Master QA, Reviews and reconciliation

2.22.1 objective=`run complete structural, security, semantic, schedule, source, public-content and replay assurance against one Master root`.

2.22.2 prerequisites=`PG20 frozen`.

2.22.3 outputs=`Producer QA; presealed Review B; Review A/B; comparison; reconciliation; successor loops`.

2.22.4 exit=`zero unresolved blocking Finding; all required domain-veto predicates satisfied; exact root stable`.

2.22.5 current=`pending`.

## 2.23 `PG22` — Exact-root human and domain approval

2.23.1 objective=`present immutable Master/Evidence/Review/Reconciliation/Veto roots to Tal and named domain authorities for the exact approval scope`.

2.23.2 prerequisites=`PG21 clean Candidate; no inferred appointments or blanket authority`.

2.23.3 outputs=`detached exact-root approvals or explicit rejection/expiry`.

2.23.4 exit=`all mandatory approvals current and root-matched; missing/rejection creates no acceptance`.

2.23.5 current=`pending; Tal’s earlier blanket continuation is not an exact-root approval`.

## 2.24 `PG23` — Gate29 protected acceptance

2.24.1 objective=`perform protected CAS/readback that makes the exact Master generation current for planning`.

2.24.2 prerequisites=`PG22 approvals; expected head; all vetoes closed; acceptance writer authority`.

2.24.3 outputs=`append-only Master Acceptance envelope; authoritative current-head readback; Planning handoff`.

2.24.4 exit=`one proven current head or conflict-blocked; response loss never assumes success`.

2.24.5 current=`BLOCKED`.

## 2.25 `PG24` — New implementation instruction and execution planning

2.25.1 objective=`after Gate29 only, request a new explicit instruction before Product or external-state work and execute only the accepted scope`.

2.25.2 prerequisites=`PG23 accepted; Public-hardening/credential/provider/legal/deploy permits separately satisfied where applicable`.

2.25.3 outputs=`ImplementationInstructionReceipt; approved execution slice; evidence-return loop to Master`.

2.25.4 exit=`defined by the future accepted Program registry and instruction; no current completion claim`.

2.25.5 current=`not authorized; Product development remains frozen`.

# 3. Current phase vector

## 3.1 Non-blended status

3.1.1 raw recovery/audit evidence phases materialized=`PG00,PG01`; accepted control phases=`0/25`.

3.1.2 active authoring/review phases=`PG02,PG03,PG05,PG06`.

3.1.3 blocked/pending phases=`PG04,PG07–PG24`.

3.1.4 the count `0/25 accepted control phases` is not Product completion and must not be converted into a Product percentage.

3.1.5 exact remaining hours and calendar time for PG00–PG24=`unknown/unavailable` because accepted Task leaves, assignments, capacities and external-wait bounds do not yet exist.

3.1.6 known ordering constraint=`PG02 → PG03 → PG04 → PG06 → PG07 → PG08 → PG09 → PG10 → PG11 → PG12 → PG13 → PG14 → PG15 → PG16 → PG17 → PG18 → PG20 → PG21 → PG22 → PG23 → PG24`; PG05 may be authored in parallel but must be accepted before PG08; PG19 blocks any Push.

3.1.7 Gate29=`BLOCKED`; development freeze=`ACTIVE`.
