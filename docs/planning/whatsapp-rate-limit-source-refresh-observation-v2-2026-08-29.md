# 1. Connect — WhatsApp official rate-limit source refresh observation v2

## 1.1 Identity and boundary

1.1.1 `observationId=CONNECT-WHATSAPP-RATE-LIMIT-SOURCE-REFRESH-2026-08-29-O2`.

1.1.2 local observed time=`2026-08-29T22:27:09+0300 IDT`; trusted Meta timestamp=`unknown/unavailable`.

1.1.3 observation class=`READ-ONLY-OFFICIAL-SOURCE-REFRESH-ATTEMPT; NOT-SOURCE-CUSTODY; NOT-LIVE-WABA-EVIDENCE; NOT-A-RATE-LIMIT-CHANGE`.

1.1.4 Tal role remains=`Research and Development owner for WhatsApp and Connect rate limiting`; this observation supplies no factual sign-off because authoritative bytes were unavailable.

1.1.5 no third-party article, search snippet, remembered limit or prior local prose may replace current official Meta bytes.

## 1.2 Official discovery attempts

1.2.1 four domain-restricted search queries were executed for throughput, messaging tiers, webhook retries and pair limits under `developers.facebook.com`.

1.2.2 official search results returned=`0`.

1.2.3 direct official URL 1=`https://developers.facebook.com/docs/whatsapp/cloud-api/overview`; result=`HTTP 429 Too Many Requests`; captured authoritative content bytes=`0`.

1.2.4 direct official URL 2=`https://developers.facebook.com/docs/whatsapp/messaging-limits`; result=`HTTP 429 Too Many Requests`; captured authoritative content bytes=`0`.

1.2.5 direct official URL 3=`https://developers.facebook.com/docs/whatsapp/cloud-api/guides/set-up-webhooks`; result=`HTTP 429 Too Many Requests`; captured authoritative content bytes=`0`.

1.2.6 direct official URL 4=`https://developers.facebook.com/docs/whatsapp/cloud-api/overview#throughput`; result=`HTTP 429 Too Many Requests`; captured authoritative content bytes=`0`.

1.2.7 result repeats the prior 2026-08-29 observation; it is not proof that the pages, limits or feature availability are unchanged.

## 1.3 Current safe policy

1.3.1 current official-source delta=`unknown/unavailable`.

1.3.2 live Business Portfolio, WABA, Phone Number, Template, quality, throughput, pair-limit and messaging-limit states=`unknown/unavailable` until authenticated provider/API/console readback and accepted custody exist.

1.3.3 the local research baseline remains `/Users/tal/Documents/connect/web/docs/whatsapp-rate-limits.md`, SHA-256=`70349a4868d3c6a9a5bc309c32a5df7317ec4e23850ca6ba6fbea55c088f2e1a`, checked=`2026-08-24`; it cannot authorize live values.

1.3.4 safe runtime design remains layered and live-derived: Connect policy cap ≤ current Meta account/number/template/quality/throughput cap; any absent, stale, contradictory or inaccessible provider state disables or reduces sending rather than assuming a remembered maximum.

1.3.5 adaptive increases require exact provider evidence, Tal factual review, expiry and rollback; automatic increase from historical documentation is forbidden.

1.3.6 HTTP 429 is handled as source-refresh failure, not as the requested application's message-send rate-limit evidence.

## 1.4 Required successor work

1.4.1 capture current official pages or immutable official assets under accepted Source Universe custody with raw digest, canonical/final URL, media type, locator, retrieval receipt and trusted-time state.

1.4.2 obtain authorized Read-only Meta API/console receipts for the exact Pilot Business Portfolio, WABA, Phone Number, Templates, quality and applicable limit capabilities without exposing credentials or customer data.

1.4.3 compare two independent readers, classify every discrepancy and bind the resulting LimitSnapshot to policy version, asset identity, observed time, expiry and invalidation triggers.

1.4.4 publish only disclosure-safe summaries; raw provider identifiers and operational Evidence remain private.

1.4.5 until then, WhatsApp provider readiness and factual limit closure remain blocked; this does not block continued Planning work.
