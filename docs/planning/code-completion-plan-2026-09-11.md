# 1. תור השלמת קוד — נספח פעיל ל־Master Plan

**מצב נוכחי R222:** שכבת מחזורי הבקשות בחיבור מחדש מומשה ואומתה מקומית: Signup חדש עם עדות ניתוק, שימור בקשות קודמות ואירועים ללא שיוך. חיבור מחדש עדיין אינו סגור: נותרו שיוך/הצגה מורשים של היסטוריה ובדיקת מסירה סופית. שני תתי־שלבים, 8–16 שעות הנדסה בביטחון נמוך; 12 שלבי ההשקה עדיין פתוחים.


1.1 הוראת Tal: להשלים תכנות לפני חיבורים. מסמך זה מפרק את יתרת הפיתוח בתוך ה־Master Plan הקיים; אינו תוכנית השקה מתחרה. בסיס הבדיקה main ‏6e26ef8b. שלבי 1–12 עדיין דורשים סגירה לפי תנאי הקבלה המקוריים.

1.2 G01–G04 ו־Dashboard כבר ממומשים. שלוש חבילות הפיתוח העיקריות שנותרו הן Knowledge, AI ו־Paddle. מתאמי ספק מקומיים ובדיקות פרוטוקול אינם הוכחה לשילוב מוצר מלא.

# 2. שלבים ותתי־שלבים לפיתוח

האומדנים בסעיף 2.1 ובסיכומי R209–R218 הם היסטוריים. יתרת העבודה המחייבת מפורטת בסעיף 17.

| ID | משימה | מצב | אומדן עבודה ראשוני | תנאי סיום מקומי |
|---|---|---|---|---|
| C1.1 | S3: העלאה מבודדת, VersionId, Receipt והסגר Knowledge | VERIFIED_LOCAL | הושלם מקומית; בסיס היה 8–16 שעות | תור עמיד, מקור מבודד, גרסה מפורשת, Hash והסגר נבדקו ב־PostgreSQL 16/17 וב־AWS SDK |
| C1.2 | סריקה, חילוץ קטעים ו־UI להעלאה/מצב/דחייה | VERIFIED_LOCAL | הושלם מקומית; בסיס היה 8–16 שעות | TXT/Markdown עד 128 KiB, סריקה של גרסה מדויקת, חילוץ אטומי וטופס עם רענון מצב |
| C2.1 | Responses, אחזור ואימות Usage וציטוטים | VERIFIED_LOCAL | הושלם מקומית; בסיס היה 4–8 שעות | מחובר ל־Worker דרך יומן עמיד; הפעלה חיה ואישור/שליחה נפרדים |
| C2.2 | יומן Generation, שריון תקציב ו־Restart/Response loss | VERIFIED_LOCAL | הושלם מקומית; בסיס היה 8–16 שעות | 16 תרחישים עברו על PostgreSQL 16/17; Usage חסר שומר אי־ודאות ושריון |
| C2.3 | אישור אדם → AI Outbox → Sender → סטטוס מסירה | VERIFIED_LOCAL | הושלם מקומית; בסיס היה 10–20 שעות | אישור מאומת, הודעה חדשה/Handoff, חלון שירות, כפילות, מסירה ומכסה משותפת נבדקו |
| C3.1 | Paddle Checkout, חתימת Webhook ושיוך Tenant | VERIFIED_LOCAL | הושלם מקומית; בסיס היה 8–16 שעות | טופס/API/Worker, ניסיון יצירה יחיד, חתימת גוף מקורי ושיוך לפי עסקה שנשמרה בשרת; PostgreSQL 16/17 |
| C3.2 | Entitlements, אירועים מאוחרים, ביטול ו־Reconciliation | VERIFIED_LOCAL | הושלם מקומית ב־R217; בסיס היה 8–16 שעות | זכאות, Portal, רכישה חוזרת ושחזור פרטי נבדקו; נדרשת קבלת ספק חיה |
| C4.1 | בדיקות מקומיות כוללות, ביקורת, Reconciliation תפעולי ומסמכי הפעלה | IN_PROGRESS | 8–16 שעות הנדסה בביטחון נמוך | שני תתי־השלבים בסעיף 16, ואחריה חיבור וקבלה חיה |

2.1 טווח הבסיס ההיסטורי לכל שמונת תתי־השלבים: 62–124 שעות הנדסה, בביטחון נמוך. זהו אומדן ראשוני לקבוצת פערי הקוד שנבדקה, לא שעות שנמדדו, לא זמן ריצה של סבב Codex ולא הבטחה לסיום המוצר. אחרי סגירה מקומית של C1.1/C1.2, C2.1/C2.2/C2.3 ו־C3.1 נותרו 2 תתי־שלבים. סכום טווחי הבסיס שלהם הוא 16–32 שעות הנדסה בביטחון נמוך; זו חיבוריות אומדני התכנון, לא חיסור שעות שנמדדו. תיקוני שילוב עשויים להוסיף עבודה.

2.2 אפשר לממש תצורה וקוד ללא חשבון ספק. מזהי מחיר/מוצר, Rate card ומודל מאומת, זכאות Meta ופרטי דומיין ייקבעו מהחשבון בשלב החיבור; אין ערכים לדוגמה במוצר. הכרעת R211: הגרסה הראשונה מקבלת TXT/Markdown בלבד עד 128 KiB. PDF/Office נדחים במפורש; הרחבת פורמטים היא תכולה עתידית, לא תכונה ממומשת.

# 3. אחרי סיום הקוד

3.1 לחבר חשבונות ומשאבים, להריץ מיגרציות ב־Staging, לפרוס גרסה אחת ב־Web/API/Worker ולהריץ קבלת QR/קוד, Upload/Scan, AI ו־Paddle Sandbox.

3.2 להשלים QA בדפדפן, Restore/Retention/ניטור, Pilot של 10 ימי עבודה ו־Canary של 72 שעות לפי ה־Master Plan. אין אומדן השקה כולל לפני מדידת השילוב; 12 שלבים ראשיים עדיין פתוחים.

3.3 [Master Plan](launch-master-plan-2026-09-09.md#82-השלמת-תכנות-לפני-חיבור-חשבונות--11092026), [הכרעות R208](launch-decisions-2026-09-09.md#7204-r208--קודם-תכנות-טיוטות-ai-לפי-מקורות-ועלויות-אמיתיות-11092026), [דוח בדיקות](../../outputs/launch-validation-2026-09-09/code-first-validation-20260911.json).


# 4. סגירה מקומית R209 — 11.09.2026

4.1 C2.1/C2.2 מאומתים מקומית: ספירת קלט, שריון התקציב, מניעת Dispatch חוזר, שמירת תוצאה ו־Usage אטומית, Replay לאחר שינוי תצורה וחיבור ה־Worker עם הפעלה מפורשת. עברו 4,540 בדיקות כלליות, שתי הבניות, TypeScript, Lint והבדיקות המתועדות. אין הפעלה חיה או פרסום מהסבב הזה.

4.2 סדר ההמשך: C2.3 אישור ושליחת תשובת AI; אחריו C1.1/C1.2 להשלמת מקורות ידע; C3.1/C3.2 ל־Paddle; ולבסוף C4.1 הכולל ביקורת מערכת, Reconciliation מתועד של חיובים לא ודאיים והוראות הפעלה. מצב uncertain אינו מתנקה אוטומטית.

4.3 נותרו 6 תתי־שלבי פיתוח ובדיקות ו־12 שלבים ראשיים. אומדן בסיס לתתי־השלבים שנותרו: 50–100 שעות הנדסה, בביטחון נמוך. זמן ההשקה הכולל נשאר לא ידוע עד חיבור וקבלה חיה. [דוח R209](../../outputs/launch-validation-2026-09-09/ai-journal-validation-20260911.json).

# 5. סגירה מקומית R210 — 11.09.2026

5.1 C2.3 מאומת מקומית: אישור אדם עם Receipt/Audit → תור עמיד → Worker → Meta text transport → הודעת Inbox ועדכון סטטוס. ברירת המחדל כבויה; אין קריאה אמיתית לספק או Deployment. תוקנו גם העברת הגדרות AI בנקודת ההפעלה ונעילת העסק בשריון/שחרור מכסות.

5.2 עברו 4,544 בדיקות כלליות, 27 בדיקות AI על PostgreSQL 16.13/17.11 ו־13 בדיקות רגרסיה של שליחה ידנית. בדיקות ספק משתמשות ב־fixtures הקיימים. [דוח R210](../../outputs/launch-validation-2026-09-09/ai-delivery-validation-20260911.json), [הוראות הפעלה](../ai-reply-delivery-runtime.md).

5.3 נותרו **5 תתי־שלבי פיתוח ובדיקות**: C1.1/C1.2 → C3.1/C3.2 → C4.1. סכום טווחי הבסיס שלהם הוא **40–80 שעות הנדסה**, בביטחון נמוך; זהו אומדן תכנון, לא יתרה שנמדדה ולא מועד השקה. **12 שלבים ראשיים** עדיין פתוחים, כולל חיבורים וקבלה חיה בסוף הפיתוח. בירור מסירה או חיוב שאינם ודאיים נשאר במסגרת C4.1.

5.4 פער בדיקות נוסף ל־C4.1: סקריפט verify-node-postgres-integration ההיסטורי מחיל רק עד migration 0053, ולכן נכשל בקריאת meta_contact_sync_states שנוספה אחר כך. הסקריפט ומתאם הקריאה זהים לבסיס R209; בדיקות AI והשליחה הידנית החדשות מחילות את כל 78 המיגרציות. יש לעדכן את סקריפט הליבה ל־Schema הנוכחי בלי לעקוף הגנות מסד נתונים.

# 6. סגירה מקומית R211 — 11.09.2026

6.1 הושלמו C1.1/C1.2 בהיקף TXT/Markdown עד 128 KiB: טופס → BFF מאומת → API → תור PostgreSQL → S3 בהסגר → סריקת גרסה → חילוץ ושמירה אטומית. עברו 10 תרחישי אינטגרציה בכל אחת מגרסאות PostgreSQL 16.13/17.11, ובדיקות AWS SDK עם תעבורה מקומית. חיבורים חיצוניים עדיין נדחים לפי הוראת Tal.

6.2 נותרו **3 תתי־שלבים**: C3.1 Checkout/Webhook, C3.2 Entitlements/Reconciliation, C4.1 בדיקות והפעלה תפעולית. בסיס האומדנים הוא **24–48 שעות הנדסה**, בביטחון נמוך; **12 שלבים ראשיים** עדיין פתוחים. אין עדיין מועד השקה מאומת.

6.3 C4.1 כולל גם טיפול תפעולי בהעלאה לא ודאית או במקור שנדחה לאחר תקלה אצל הספק, ומדיניות מחיקה ל־S3 בנפרד מניקוי הקובץ הזמני במסד. אין לבצע PUT חוזר או למחוק קובץ על סמך היעדר תשובה בלבד. [הוראות ההפעלה והמגבלות](../knowledge-ingestion-runtime.md).


# 7. סגירה מקומית R212 — 11.09.2026

7.1 C3.1 הושלם מקומית: הכנת עסקה בידי Owner → BFF/API מאומת → תור PostgreSQL → ניסיון Paddle יחיד → פתיחת העסקה השמורה ב־Paddle.js. נוספו Webhook עם חתימת הגוף המקורי, Receipt עמיד ושיוך מנוי רק באמצעות העסקה שנוצרה ונשמרה בשרת. אירועים מאוחרים/כפולים וגרסאות ברמת Microsecond נבדקו על PostgreSQL 16.13/17.11.

7.2 **כל התכנות עדיין לא הושלם.** ב־C3.2 מומשה שמירת עובדות המנוי, אך לא אכיפת הרשאות בתשלום, Portal לניהול/ביטול, רכישה נוספת או תיקון תפעולי של יצירה לא ודאית. אין פתיחת חיוב ללקוחות בסבב הזה. [פירוט ומגבלות](../paddle-checkout-runtime.md).

7.3 תתי־שלבי ההמשך בתוך שתי חבילות הסגירה:

| חבילה | תתי־משימות שנותרו | אומדן בסיס כולל לחבילה |
|---|---|---|
| C3.2 | 1. מדיניות זכאות תוך שמירת גישה לחיוב; 2. אכיפה ב־API ובפעולות Worker; 3. Portal וביטול/תשלום; 4. טיפול בלקוח משלם למספר עסקים ורכישה חוזרת; 5. Reconciliation עם ראיות, כולל יצירה לא ודאית | 8–16 שעות |
| C4.1 | 1. חיבור Readiness אמיתי לפרסום AI; 2. בירור עלות/מסירה/העלאה לא ודאיות; 3. Retention ומחיקת S3; 4. עדכון בודק PostgreSQL ההיסטורי; 5. רגרסיה, ביקורת ומסמכי הפעלה | 8–16 שעות |

7.4 נותרו **2 חבילות פיתוח ובדיקות ו־12 שלבים ראשיים**. שתי החבילות כוללות את תתי־המשימות בטבלה; אין פירוש הדבר שנותרו שתי פעולות בלבד. סכום אומדני הבסיס הוא **16–32 שעות הנדסה**, בביטחון נמוך, ולא זמן שנמדד או תאריך השקה. החיבורים וקבלת הספק נדחים לסוף הפיתוח לפי הוראת Tal.

# 8. R213 — אכיפת זכאות, 11.09.2026

8.1 הושלמו מקומית תתי־משימות C3.2.1 (מדיניות זכאות) ו־C3.2.2 (אכיפה ב־API וב־Worker). נבדקות תקופה, ביטול מתוזמן, טריות אימות, סתירה ושינוי הרשאה בזמן המתנה. Checkout Production מאמץ מדיניות בתשלום באופן עמיד; אין חזרה ל־Pilot באמצעות כיבוי Worker או מחיקת שיוך. נשמרת גישה למידע, לחיוב ולעצירת פעילות.

8.2 נותרו **8 תתי־משימות בתוך 2 חבילות**: C3.2.3 ניהול/ביטול/עדכון תשלום; C3.2.4 משלם משותף ורכישה חוזרת; C3.2.5 בירור יצירה/עובדות לא ודאיות; וחמש תתי־משימות C4.1 שבטבלה בסעיף 7.3. C3.2 אינו סגור וכלל התכנות טרם הושלם.

8.3 נותרו **12 שלבים ראשיים להשקה**. בסיס האומדנים לחבילות הוא עדיין **16–32 שעות הנדסה בביטחון נמוך**, ללא מדידת יתרה חדשה. אין תאריך השקה מאומת. החיבורים נדחים לסיום הפיתוח לפי הוראת Tal. [מימוש ומגבלות](../paddle-paid-access-runtime.md), [דוח בדיקות R213](../../outputs/launch-validation-2026-09-09/paid-access-validation-20260911.json).

# 9. R214 — ניהול מנוי ורכישה חוזרת, 11.09.2026

9.1 הושלמו מקומית C3.2.3/C3.2.4: קישור לפורטל Paddle עם אימות הדוא״ל של המשלם, הצגת מזהה המנוי ומועד שינוי מתוזמן, תמיכה במשלם משותף לעסקים שונים ורכישה חדשה לאחר ביטול סופי מאומת. בקשה חוזרת קשורה לדור הרכישה שנצפה; היסטוריה אינה נמחקת ואינה מעניקה זכאות למנוי החדש. אלה מימוש ובדיקות מקומיים, לא קבלת שירות חי.

9.2 **כל התכנות עדיין לא הושלם.** מסלול בירור יצירה לא ודאית, עסקה שנדחתה/בוטלה לפני השלמת תשלום וסתירת עובדות נשאר ב־C3.2.5. אין איפוס אוטומטי של unknown ואין שיוך לפי כתובת דוא״ל או ניחוש מזהה.

9.3 תוכנית הסיום שנותרה, לפי סדר ביצוע:

| שלב | תתי־שלבים ותנאי סיום | אומדן הנדסי מעודכן |
|---|---|---|
| C3.2.5 | איסוף ראיות ספק → הכרעת מפעיל מורשה → רישום בלתי־משתנה → בדיקות חזרה, סתירה ותוצאה לא ודאית | 4–8 שעות |
| C4.1.1 | חיבור Readiness אמיתי לפרסום AI → בדיקת תצורה, מדיניות ומסלול Handoff → בדיקות חסימה ופתיחה | 2–4 שעות |
| C4.1.2 | בירור עלות AI, מסירת הודעה והעלאה לא ודאיות → אימות ראיות → שמירת Settlement ללא שליחה חוזרת | 2–4 שעות |
| C4.1.3 | Retention ומחיקת גרסאות S3 → הרשאות וסדר מחיקה → בדיקת כשל/חזרה | 1–2 שעות |
| C4.1.4 | עדכון בודק PostgreSQL ההיסטורי לכל הסכמה → התאמת Fixtures → מעבר מלא בלי עקיפת Guards | 1–2 שעות |
| C4.1.5 | ביקורת כוללת → רגרסיה → מסמכי הפעלה ורשימת חיבור מרוכזת | 2–4 שעות |

9.4 נותרו **6 תתי־משימות בתוך 2 חבילות תכנות**, באומדן מעודכן של **12–24 שעות הנדסה בביטחון נמוך**. זהו אומדן עבודה לפי ההיקף שנותר, לא זמן ריצה שנמדד ולא מועד השקה. **12 שלבים ראשיים** עדיין פתוחים. לאחר הקוד: חיבורי חשבונות → Staging ותצורה אחידה → קבלת QR/מדיה/AI/Paddle → Restore/ניטור → Pilot ו־Canary לפי ה־Master Plan. חלונות הקבלה נשארים 10 ימי עבודה ו־72 שעות; אין תאריך השקה מאומת.

9.5 אימות: 4,573 בדיקות כלליות ושתי הבניות; 24 תרחישי Paddle בכל אחת מגרסאות PostgreSQL 16.13/17.11 כולל שדרוג נתונים קיימים; עוד 56 תרחישי Worker על PostgreSQL 17.11. TypeScript/Lint/Source/Interface עברו; 28 אזהרות Lint קיימות. [דוח R214](../../outputs/launch-validation-2026-09-09/paddle-lifecycle-validation-20260911.json), [מימוש ומגבלות](../paddle-subscription-lifecycle.md).

# 10. R215 — שחזור Checkout ותיקון שילוב PostgreSQL, 11.09.2026

10.1 מומשו ונבדקו מקומית: Seal עמיד חד־פעמי לפני יצירת עסקה; סגירת ניסיון שלא הגיע ל־Seal; שמירת מזהה מהתשובה המקורית לפני פענוח עסקי; שחזור ב־GET בלבד; וסגירת עסקה שביטולה אומת ללא מנוי. ניסיונות ישנים משודרגים בזהירות כ־sealed. היסטוריה וראיות אינן ניתנות לשינוי/מחיקה, ורכישה נוספת דורשת פעולה מפורשת בדור הנכון. [המימוש, המגבלות והמקורות](../paddle-checkout-recovery.md).

10.2 בודק PostgreSQL ההיסטורי מחיל כעת את כל 83 המיגרציות. תוקנו Inventory מדויק של 65 Triggers בהעברת מדיניות WhatsApp, נעילה אטומית ברישום הרשאת Staging, ונעילת עסק לפני Claim/Deferral/Acceptance/סטטוס מסירה של Bot. הותאמו מזהי Meta וגרסאות צפויות ל־Fixtures הקיימים. הבודק עדיין נכשל בתרחיש דחייה ישן ללא הוכחת גבול ספק; לא הוסרה דרישת ההוכחה ולא סומנה הבדיקה כעוברת. נדרשת התאמת Fixture שלם, כולל הצרכנים שלו ביומן Staging, לפרוטוקול 0055–0057.

10.3 כל התכנות טרם הושלם. סדר העבודה ותנאי הסיום המעודכנים:

| שלב | יתרה ותנאי סיום | אומדן שעות הנדסה |
|---|---|---|
| C4.1.4 | Fixture ספק לפי Credential/Session barrier → הוכחות דחייה וחלון שירות → יומן Staging → מעבר מלא על הסכמה הנוכחית | 3–6 |
| C3.2.5 | בירור unknown ללא מזהה מקורי → הכרעה מורשית מתועדת; בירור סתירת מנוי → אישור לגרסה מדויקת ללא עקיפת זכאות | 2–4 |
| C4.1.1 | Readiness אמיתי לפרסום AI → תצורה, מדיניות ו־Handoff → חסימה ופתיחה מאומתות | 2–4 |
| C4.1.2 | בירור עלות/מסירה/העלאה לא ודאיות → ראיות ו־Settlement ללא בקשת ספק כפולה | 2–4 |
| C4.1.3 | Retention ומחיקת גרסאות S3 → סדר מחיקה והרשאות → כשל וחזרה | 1–2 |
| C4.1.5 | ביקורת כוללת → רגרסיה סופית → הוראות הפעלה ורשימת חיבורים מרוכזת | 2–4 |

10.4 נותרו **6 תתי־משימות בשתי חבילות**, **12–24 שעות הנדסה בביטחון נמוך**. הטווח הכולל נשאר, אך חלק מאומדן שחזור התשלום עבר לעדכון בדיקות הליבה בעקבות הממצאים. זה אינו זמן שנמדד או הבטחת מועד. **12 שלבים ראשיים** עדיין פתוחים. אחרי הקוד: חיבורי חשבונות → Staging ותצורה אחידה → QR/מדיה/AI/Paddle חיים → Restore/ניטור → Pilot של 10 ימי עבודה ו־Canary של 72 שעות. אין תאריך השקה מאומת.

10.5 עברו 34 תרחישי Paddle בכל PostgreSQL 16.13/17.11, כולל שדרוג נתונים ישנים, ו־56 תרחישי AI/Knowledge/Manual על 16.13; בכל המסדים הוחלו 83 מיגרציות. בדיקת הליבה ההיסטורית אינה כלולה במניין ההצלחות. [דוח R215](../../outputs/launch-validation-2026-09-09/paddle-recovery-validation-20260911.json).

# 11. R216 — פרסום AI לפי תנאים עדכניים, 11.09.2026

11.1 C4.1.1 נסגר מקומית. ה־API קורא דיווח Worker מוגבל בזמן ללא סודות ספק. פרסום בודק זכאות ומשתמש נוכחיים, USD, אישור נציג ומקורות מוכנים; הוא מתבצע עם Audit ו־Receipt באותה טרנזקציה, וכולל בדיקה חוזרת אחרי המתנה. ממשק שלוש השפות הותאם להיקף הנתמך. [מימוש, תפעול ומגבלות](../ai-publication-readiness.md).

11.2 C4.1.4 נבדק תחילה ברמת הקוד: תרחישי דחיית ספק ויומן Staging תלויים זה בזה ובפרוטוקול הישן. לא שונו ציפיות כדי להכשיר מעבר. הוקדם C4.1.1, שאינו תלוי בהם, כדי להשלים פער מוצר עצמאי. בדיקת הליבה נשארת פתוחה; כישלון R215 אינו מסומן כפתור.

| שלב | סדר המשך ותנאי סיום | אומדן שעות הנדסה |
|---|---|---|
| C4.1.4 | הוכחת ספק לפי Credential/Session barrier → התאמת כל צרכני ה־Fixture → מעבר מלא על הסכמה הנוכחית | 3–6 |
| C3.2.5 | בירור Checkout ללא מזהה מקורי → הכרעה מורשית מתועדת; פתרון סתירת מנוי לגרסה מדויקת | 2–4 |
| C4.1.2 | בירור עלות AI, מסירה והעלאה לא ודאיות → ראיות ו־Settlement בלי בקשת ספק כפולה | 2–4 |
| C4.1.3 | Retention ומחיקת גרסאות S3 → סדר והרשאות → כשל וחזרה | 1–2 |
| C4.1.5 | ביקורת כוללת → רגרסיה סופית → הוראות הפעלה ורשימת חיבורים מרוכזת | 2–4 |

11.3 נותרו **5 תתי־משימות בתוך 2 חבילות**, באומדן **10–20 שעות הנדסה בביטחון נמוך**. הטווח חושב מסכום המשימות הפתוחות; זו אינה מדידת זמן או הבטחת סיום. **12 שלבי ההשקה הראשיים** נשארים פתוחים. לאחר הקוד: חיבורים → Staging אחיד → QR/מדיה/AI/Paddle חיים → Restore וניטור → Pilot של 10 ימי עבודה ו־Canary של 72 שעות. אין תאריך השקה מאומת.

11.4 עברו 10 תרחישי פרסום בכל PostgreSQL 16.13/17.11 ועוד 56 תרחישי AI/Knowledge/Manual ב־17.11, כולם עם 84 מיגרציות. אין חיבור חדש, בקשת ספק או פריסה. [ראיות R216](../../outputs/launch-validation-2026-09-09/ai-readiness-validation-20260911.json).


# 12. R217 — סגירת שחזור Paddle תפעולי, 11.09.2026

12.1 C3.2.5 הושלם מקומית: כלי CLI פרטי להכנה/בדיקה/החלה, הרשאת מסד לפי משתמש אמיתי/עסק/סביבה ותפוגה, רישום בלתי־משתנה ושינוי אטומי. מסלולים: שיוך מזהה שהוכח בבירור הספק; סגירה לאחר אישור סופי שלא נוצרה עסקה; הכרעה בסתירת מנוי לגרסה מדויקת. כל פעולה נשענת על ראיות מפעיל שנשמרות מחוץ למאגר. אין זיהוי לפי דוא״ל, איפוס Seal או POST חוזר. [הוראות ומגבלות](../paddle-operator-recovery.md).

12.2 אומת משתמש מסד נפרד ומוגבל שאינו יכול להעניק לעצמו הרשאה. עברו 47 תרחישי Paddle בכל PostgreSQL 16.13/17.11, על כל 85 המיגרציות. רגרסיית AI/Knowledge/Manual/Publication נבדקת בנפרד בדוח. קבלה חיה לא בוצעה; כלי השחזור אינו יכול לאמת בעצמו את אמיתות עדות התמיכה האנושית.

| שלב | יתרה ותנאי סיום | אומדן שעות הנדסה |
|---|---|---|
| C4.1.4 | התאמת Fixture גבול ספק וכל צרכני יומן Staging → מעבר בודק הליבה בלי עקיפת Guards | 3–6 |
| C4.1.2 | בירור עלות AI, מסירה והעלאה לא ודאיות → ראיות ושמירת תוצאה ללא בקשת ספק כפולה | 2–4 |
| C4.1.3 | Retention ומחיקת גרסאות S3 → הרשאות וסדר מחיקה → כשל וחזרה | 1–2 |
| C4.1.5 | ביקורת כוללת → רגרסיה סופית → מסמכי הפעלה ורשימת חיבורים מרוכזת | 2–4 |

12.3 נותרו **4 תתי־משימות בחבילת C4.1**, **8–16 שעות הנדסה בביטחון נמוך**. זו הערכת היקף, לא זמן ריצה שנמדד או הבטחת מועד. **12 שלבי ההשקה הראשיים** נשארים פתוחים. לאחר הקוד: חיבורי חשבונות → Staging אחיד → QR/מדיה/AI/Paddle חיים → Restore וניטור → Pilot של 10 ימי עבודה ו־Canary של 72 שעות. אין תאריך השקה מאומת. [ראיות R217](../../outputs/launch-validation-2026-09-09/paddle-operator-validation-20260911.json).


# 13. R218 — סגירת Retention לקובצי ידע, 11.09.2026

13.1 C4.1.3 הושלם מקומית: סגירת עסק לפי שעון המסד → תקופת שמירה מוגדרת → בדיקת עיכוב משפטי מתועדת → הכנת הצעה → הוצאה משימוש וכתיבת משימה אטומית → מחיקת גרסת S3 המדויקת. כלי פרטי, משתמש מסד מזוהה והרשאת AWS נפרדת; אין מחיקה אוטומטית מה־API או מעובד ההעלאה. מקור שיצא משימוש אינו מופעל מחדש. [הוראות תפעול ומגבלות](../knowledge-object-retention.md).

13.2 הבדיקות כוללות משתמש מוגבל שאינו יכול להעניק לעצמו הרשאה, שינוי עיכוב משפטי, ביטול הרשאה, הצעות שהתיישנו, מקביליות, אובדן אישור ו־Rollback. תוקנה הרשאת כתיבה מיותרת ביומן ההעלאות ונחסמה תצורת TLS כפולה בשני כלי התפעול הפרטיים. עברו 26 תרחישי Knowledge בכל PostgreSQL 16.13/17.11, 102 תרחישי AI/Publication/Manual/Paddle ב־17.11, 4,588 בדיקות כלליות ושתי הבניות. בכל מסד נבדקו 86 מיגרציות; בדיקת הליבה ההיסטורית אינה כלולה במעברים. [דוח R218](../../outputs/launch-validation-2026-09-09/knowledge-retention-validation-20260911.json).

| סדר | שלב | תתי־שלבים ותנאי סיום | אומדן שעות הנדסה |
|---|---|---|---|
| 1 | C4.1.4 | עדכון הוכחת גבול הספק → התאמת צרכני Fixture ויומן Staging → מעבר בודק הליבה בכל הסכמה בלי עקיפת Guards | 3–6 |
| 2 | C4.1.2 | בירור עלות AI, מסירת הודעה והעלאה לא ודאיות → אימות ראיות והרשאה → שמירת תוצאה ללא שליחת בקשת ספק כפולה | 2–4 |
| 3 | C4.1.5 | ביקורת כוללת → תיקון ממצאים → רגרסיה סופית → הוראות הפעלה ורשימת חיבורים מרוכזת | 2–4 |

13.3 נותרו **3 תתי־משימות בחבילת C4.1**, באומדן **7–14 שעות הנדסה בביטחון נמוך**. הטווח הוא סכום אומדני התכנון שנותרו, לא שעות שנמדדו ולא הבטחת זמן ריצה. **12 שלבי השקה ראשיים** נשארים פתוחים. לאחר הקוד: חיבורי חשבונות → Staging אחיד → קבלת QR/מדיה/AI/Paddle → Restore וניטור → Pilot של 10 ימי עבודה → Canary של 72 שעות. משך החיבורים והקבלה עדיין לא ידוע; אין תאריך השקה מאומת.

13.4 מחיקת S3 אינה מחיקת כלל תוכן העסק: קבלות ההעלאה, הקטעים שחולצו וה־Audit נשמרים לפי מדיניותם הנפרדת. מקור ללא VersionId ודאי נשאר לבירור C4.1.2. אין פעולה חיה ב־AWS או פרסום מהסבב הזה; קבלה אצל הספק תתבצע לאחר החיבור.


# 14. R219 — סגירת בודק PostgreSQL ותיקון אישורי מסירה, 12.09.2026

14.1 C4.1.4 הושלם מקומית: כל 86 המיגרציות נבדקו ב־PostgreSQL 16.13 וב־17.11. בכל גרסה עברו 92 תרחישים בסכמה הנוכחית ו־9 תרחישי פרוטוקול היסטורי בסכמה המתאימה לו, ולאחריהם הושווה שימור רשומות הפעולה והתוצאה לאורך השדרוג. בדיקות Bot משתמשות בהרשאה, Credential binding, מפתחות מכסה אמיתיים של ה־Fixture, Permit והוכחת Session לפני גבול הספק. לא הוחזר לפעולה מסלול הספק הישן.

14.2 תוקנה תקלה בקמפיינים: רישום אישור וקבלת סטטוס ספק נועלים כעת את העסק לפני נעילות השורות ושינוי המכסה. בדיקות ההרשאות ממשיכות לחסום שימוש בקוד הבדיקה מתוך Runtime וגישת SQL ישירה למנגנונים הרדומים. [הפעלת בודק הליבה](../postgresql-core-verifier.md), [דוח R219](../../outputs/launch-validation-2026-09-09/core-current-protocol-validation-20260912.json).

| סדר | שלב | תתי־שלבים ותנאי סיום | אומדן שעות הנדסה |
|---|---|---|---|
| 1 | C4.1.2 | בירור עלות AI, מסירה והעלאה לא ודאיות → אימות ראיות והרשאה → שמירת תוצאה בלי בקשת ספק כפולה | 2–4 |
| 2 | C4.1.5 | ביקורת כוללת → תיקון ממצאים → רגרסיה סופית → הוראות הפעלה ורשימת חיבורים מרוכזת | 2–4 |

14.3 נותרו **2 תתי־משימות בחבילת C4.1**, **4–8 שעות הנדסה בביטחון נמוך**. הטווח הוא סכום אומדני התכנון, לא זמן ריצה מדוד או מועד מובטח. **12 שלבי ההשקה הראשיים** עדיין פתוחים. לאחר הקוד: חיבור חשבונות → Staging אחיד → קבלת QR/מדיה/AI/Paddle → Restore וניטור → Pilot של 10 ימי עבודה → Canary של 72 שעות. אין מועד השקה מאומת.


# 15. R220 — סגירת תוצאות לא ודאיות, 12.09.2026

15.1 C4.1.2 הושלם מקומית. ארבעה כלי שחזור מטפלים בעלות AI, מסירת AI, מסירה ידנית וגרסת Knowledge. ההכרעה היא תוספת ראיה; אין איפוס שיגור או בקשת ספק כפולה. Usage מאוחר פותח בירור בסתירה, אישור הודעה מאוחר נשמר, ו־Knowledge עובר שוב סריקה לפני שימוש. [תפעול, הרשאות ומגבלות](../uncertain-outcome-recovery.md).

15.2 נמצאה ותוקנה חסימת שיחה לאחר מסירה ידנית לא ודאית: נוספה הכרעה פרטית מתועדת עם הרשאה נפרדת, שדרוג מתוך unknown וקליטת אישור מאוחר. עלות, מסירה והעלאה אינן חולקות סמכות מפעיל אוטומטית. כל 90 המיגרציות נבדקו, ונשמרו בדיקות שדרוג הפרוטוקול הישן.

| סדר | תת־שלב שנותר | עבודה ותנאי סיום | אומדן |
|---|---|---|---|
| 1 | C4.1.5.1 | להשוות את W01–W21 והתוכניות הפעילות לקוד; בפרט לאמת אילו פערי מדיה/סנכרון הם קוד ואילו קבלה חיה | בתוך אומדן הביקורת |
| 2 | C4.1.5.2 | לתקן ממצא קוד מהותי שנמצא ולבדוק אותו; לא למחוק דרישה כדי להכריז סיום | ייקבע לפי ממצאים |
| 3 | C4.1.5.3 | לאמת גרסת מסירה אחת, הרשאות, רגרסיה ומסמכי הפעלה; לשמור חבילת שינויים וראיות | בתוך אומדן הביקורת |

15.3 נותר **תת־שלב ראשי אחד בחבילת C4.1**, הכולל את שלוש הפעולות לעיל. אומדן **2–4 שעות לביקורת בביטחון נמוך**, ולא הבטחה לכל התכנות אם יימצאו פערים. אומדן R219 של 4–8 שעות תוקן במהלך המימוש ל־6–12 בגלל מורכבות הראיות המאוחרות והמסירה הידנית; אלה אומדני תכנון, לא שעות שנמדדו.

15.4 **12 שלבי ההשקה הראשיים עדיין פתוחים**. [רשימת החיבורים המרוכזת](service-connections-handoff-2026-09-12.md) מוכנה להמשך לאחר הקוד. Staging, קבלת QR/מדיה/AI/Paddle, Restore וניטור, Pilot של 10 ימי עבודה ו־Canary של 72 שעות אינם מוכחים מקומית. אין תאריך השקה מאומת. [דוח R220](../../outputs/launch-validation-2026-09-09/uncertain-recovery-validation-20260912.json).


# 16. R221 — ניקוי מדיה והכרעת מדיניות הייבוא, 12.09.2026

16.1 A221-3/W10 נסגר בקוד המקומי: איתור גרסאות S3 תחת מפתח מדויק, HEAD המאמת שיוך, Retention מפורש לפי גיל או סגירת עסק, בדיקת עיכוב משפטי, הכנה לחמש דקות ותור מחיקה לכל גרסה. `sweep` מבצע בחירה ומחיקה אוטומטיות לפי מדיניות והרשאה פרטית, עד עשרה מפתחות ועשרים גרסאות בכל קריאה, עם סמן המשך. אין איפוס אוטומטי של ניסיונות שמוצו. תזמון אצל ספק, IAM וערכי המדיניות הם שלב ההפעלה. [הוראות המפעיל](../meta-media-retention.md).

16.2 A221-1 אומת מחדש: שינוי כיתוב תואם מאפשר בדיקת העותק המקורי שכבר נשמר; הוא אינו פותח הורדה/העלאה נוספת. ניתוק, שינוי הסכמה או סתירה ממשיכים לחסום. הבדיקה כלולה בסדרת המדיה/היסטוריה עם הסכמה הנוכחית.

16.3 הכרעת A221-4: `progress=100` הוא סימון סיום מצד Meta; לאחר עיבוד הנתונים שהגיעו נשמור את הניסוח המדויק הקיים — הנתונים שהתקבלו עובדו, השלמות טרם אומתה. לא נוסיף סימון שכל ההיסטוריה התקבלה על בסיס היעדר עבודה בתור. התיעוד הרשמי נקרא בהצלחה דרך עמוד Markdown לאחר שכשל בכלי החיפוש; אין בו מזהה בקשת סנכרון בכל אצוות היסטוריה. קבלה מול חשבון אמיתי נשארת שלב נפרד. [Meta — מחזור הסנכרון](https://developers.facebook.com/documentation/business-messaging/whatsapp/embedded-signup/onboarding-business-app-users/).

| סדר | מזהה | יתרת הביצוע | אומדן שעות הנדסה |
|---|---|---|---:|
| 1 | C4.1.5.2b / A221-2 | לשמר מחזור קודם וראיותיו → לקשור השלמת Signup חדשה למחזור חדש → למנוע שיוך אצוות מאוחרות לפי זמן הגעה → לאמת חידוש, סירוב, קריאות היסטוריה ו־Restart | 6–12 |
| 2 | C4.1.5.3 | ביקורת יתרת W01–W21 → רגרסיה סופית על גרסת מסירה אחת → השלמת Runbook, מטריצת הרשאות וחבילת שינויים מקומית | 2–4 |

16.4 נותרו **שני תתי־שלבים בתוך C4.1.5**, באומדן **8–16 שעות הנדסה בביטחון נמוך**. האומדן התרחב לעומת אומדן הביקורת משום שנמצא פער קוד אמיתי בחיבור מחדש. זו אינה מדידה או התחייבות לזמן שעון. A221-2 אינו בעיית התחברות לחשבון ואינו נמחק כדי להכריז סיום. התיעוד מאשר שסנכרון נוסף מחייב Offboarding ולאחריו Signup חדש; החלפת ההרשאה לבדה אינה מתירה Replay של הבקשה הקודמת.

16.5 **12 שלבי השקה עדיין פתוחים.** לאחר הקוד: חיבורי שירותים → Staging אחיד → קבלת QR/מדיה/AI/Paddle → Restore וניטור → Pilot של 10 ימי עבודה → Canary של 72 שעות. משך החיבורים ואישורי הספק אינו מאומת, ולכן אין תאריך השקה. [דוח R221](../../outputs/launch-validation-2026-09-09/media-retention-validation-20260912.json).


# 17. R222 — מחזורים נפרדים ושימור קליטה ללא שיוך, 12.09.2026

17.1 הושלמו רכיבי A221-2: מפתחות סנכרון לכל מחזור, Signup חדש עם Receipt וגרסת בסיס, עדות Offboarding מחייבת כשכבר שוגרה בקשה, המשך Worker אחרי Restart, תוצאות מאוחרות במחזור המקורי ו־Maintenance מוגבל למחזור אחד. התחלה ישנה שלא שיגרה בקשה נשמרת בנפרד, ללא שינוי תאריך או זהות.

17.2 אירועים ללא מזהה מחזור מוכח נשמרים בנפרד ואינם משנים Inbox או שמות אנשי קשר. סירוב משחיר את התוכן שבבירור ואינו מאפשר שחזור אוטומטי. תוקנו נעילה רחבה מדי במסלול המדיה והתנגשות נעילה בין Maintenance ל־Audit של תוצאה מאוחרת. [מימוש והרשאות](../meta-sync-generations.md).

| סדר | תת־שלב שנותר | תנאי סיום | אומדן שעות הנדסה |
|---|---|---|---:|
| 1 | A221-2, יתרת ההיסטוריה בחיבור מחדש | מדיניות ופעולה מורשית לקריאת היסטוריה קודמת; שיוך אצוות מוכח ומימוש ייבוא מחזור חדש בלי לשנות את הישן; סירוב, סתירה ו־Restart | 6–12 |
| 2 | C4.1.5.3, מסירה סופית | ביקורת W01–W21 על גרסת מסירה אחת, רגרסיה, Runbook, הרשאות וחבילת שינויים | 2–4 |

17.3 נותרו **שני תתי־שלבים, 8–16 שעות הנדסה בביטחון נמוך**. התשתית התקדמה אך יכולת ייבוא ההיסטוריה לאחר חיבור מחדש עדיין אינה שלמה; אין מסלול שחרור ליומן הבירור בקוד הנוכחי. האומדן הוא תכנון, לא זמן ריצה מדוד או מועד מובטח. **12 שלבי ההשקה** הכוללים חיבורים, Staging, קבלה חיה ו־Pilot נשארים פתוחים.

17.4 [דוח R222](../../outputs/launch-validation-2026-09-09/sync-generations-validation-20260912.json). אין פריסה, בקשת Meta חיה או פרסום בסבב הזה.
