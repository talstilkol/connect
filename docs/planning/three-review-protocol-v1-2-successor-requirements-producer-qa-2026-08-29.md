# 1. Connect — Three-review Protocol v1.2 successor requirements Producer QA

## 1.1 Identity and authority limit

1.1.1 `artifactId=CONNECT-THREE-REVIEW-PROTOCOL-V1-2-SUCCESSOR-REQUIREMENTS-PRODUCER-QA-2026-08-29`.

1.1.2 exact subject path=`/Users/tal/Documents/connect/web/docs/planning/three-review-protocol-v1-2-successor-requirements-2026-08-29.md`.

1.1.3 exact subject raw SHA-256=`90d3a33aa11204afc6ea1fc92ba31acf615729617470b3137418a71a618c6461`.

1.1.4 subject physical identity=`563 lines/44966 bytes/UTF-8`.

1.1.5 QA role=`Producer mechanical and coverage inspection`; independence=`NO`; semantic Acceptance authority=`NONE`.

1.1.6 this QA does not accept the requirements, create a Protocol, normalize any Finding, close any source Finding or authorize Product Code, Git, Push, Deployment, provider action or Gate credit.

# 2. Mechanical checks

## 2.1 Requirement shape

2.1.1 Requirement headings=`35`; unique Requirement IDs=`35`; required exact sequence=`MPRR-001` through `MPRR-035`; observed gaps=`0`; duplicate Requirement IDs=`0`.

2.1.2 `rule` fields=`35`; `causeAndEffect` fields=`35`; `sourceIds` fields=`35`; `acceptancePredicate` fields=`35`; `dependencies` fields=`35`.

2.1.3 duplicate numbered clause IDs=`0`.

2.1.4 dependency edges=`129`; unknown dependency targets=`0`; self edges=`0`; duplicate edges within a Requirement=`0`; detected cycles=`0`.

## 2.2 Explicit source-identity checks

2.2.1 mathematical-hostile-review Finding IDs represented literally=`22/22`; required exact set=`MPRR-MATH-HR-F001` through `MPRR-MATH-HR-F022`; shorthand ranges or suffix-only IDs=`0`.

2.2.2 Intake defect IDs represented literally=`12/12`; required exact set=`INTAKE-E001` through `INTAKE-E012`.

2.2.3 Section 9 contains one explicit forward mapping for each of the 22 mathematical-review Findings and one forward/inverse mapping for each of the 12 Intake defects.

2.2.4 direct identity coverage is navigation and accountability only; it receives zero semantic-closure credit until independent reviewers prove each mapped rule, vector and terminal sufficient.

# 3. Producer semantic inspection

## 3.1 Corrections intentionally introduced

3.1.1 external Bootstrap authority, conformance-only generations and a detached `ProtocolUsePermit` replace the circular use of an unaccepted Protocol.

3.1.2 Review-envelope identity is detached from its 17-field payload and cannot hash itself.

3.1.3 Run Request identity is separated from Result identity and append-only status/attempt receipts.

3.1.4 one serialization pipeline connects validated records, collection normalization, canonical JSON, domain framing and digest construction.

3.1.5 collection types distinguish ordered lists, mathematical Sets and multisets; Set order is canonical and duplicate handling is fail-closed.

3.1.6 all 73 prior local observations remain immutable `LegacyObservation` records; none can be retroactively upgraded to eligible Review Evidence.

3.1.7 missing predicates require a separately authorized Reviewer Amendment or Re-observation; a Normalizer cannot invent them.

3.1.8 raw local identity, normalized identity and Alias edges are separate typed domains.

3.1.9 Normalizer independence now covers actor, code, parser, toolchain, dependency, environment, fixture and information-flow roots.

3.1.10 equivalence requires identical canonical key bytes and full digests; a full-digest collision blocks instead of merging.

3.1.11 local observations are partitioned into disjoint Legacy, Eligible and Blocked Sets; only Eligible records map to Semantic Findings.

3.1.12 Comparison now has an eligibility-aware presence vector, exact field universe, deterministic value groups and participant cardinality.

3.1.13 identity-changing Resolution creates a semantic successor and cannot mutate or transfer status under the prior identity.

3.1.14 invalidation uses a closed typed acyclic graph and a time-bound `Fresh(object,t)` predicate.

3.1.15 protected Acceptance now defines operation identity, idempotency, authority epoch, fencing, sequence, linearization and operation-bound readback.

3.1.16 P0/P1 Findings cannot be bypassed through Protocol risk acceptance.

3.1.17 two conformance generations require a controlled Delta, stale-A attack, exact affected Set, Generation-B recovery and offline replay.

## 3.2 Residual limits

3.2.1 these are prose-level requirements, not JSON Schemas, canonical byte vectors, two implementations, Graph engines, normative fixtures, Review envelopes or live Protocol Evidence.

3.2.2 the current requirement root has not received independent structural, mathematical or security-semantic review.

3.2.3 the 22 predecessor Findings remain open until an independent closure process against this exact subject root concludes; Producer judgment cannot close them.

3.2.4 the current three historical Master reviews remain formally ineligible under the future Protocol and may only serve as immutable Legacy input or trigger a new authorized Re-observation.

3.2.5 exact semantic Finding denominator, Product completion, remaining person-hours, critical path and calendar ETA remain `unknown/unavailable`.

# 4. QA disposition

## 4.1 Result and next safe action

4.1.1 mechanical result=`PASS-ON-RECORDED-CHECKS`.

4.1.2 Producer semantic result=`CORRECTION-INTENT-MATERIALIZED; NOT-INDEPENDENTLY-PROVED`.

4.1.3 accepted requirements=`0/35`; accepted mathematical-review mappings=`0/22`; accepted Intake mappings=`0/12`.

4.1.4 next safe action=`freeze the exact subject root and run independent hostile mathematical/identity, structural/testability and security/authority reviews; do not author or use a Protocol until all blocking findings have an accepted successor disposition`.

4.1.5 `Gate29=BLOCKED`; `development freeze=ACTIVE`; no implementation authority is created.
