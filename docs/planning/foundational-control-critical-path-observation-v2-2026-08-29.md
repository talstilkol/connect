# 1. Connect — Foundational control critical-path observation v2

## 1.1 Identity and claim limit

1.1.1 `observationId=CONNECT-FOUNDATIONAL-CRITICAL-PATH-2026-08-29-O2`.

1.1.2 `observationClass=PLANNING-STATUS-AND-ORDER; NOT-AN-ACCEPTED-PHASE-DENOMINATOR; NOT-A-PRODUCT-METRIC`.

1.1.3 observed date=`2026-08-29 Asia/Jerusalem`; exact trusted timestamp=`unknown/unavailable`.

1.1.4 validity=`until any listed Subject, review, successor or authority state changes`.

1.1.5 repository visibility=`PUBLIC-AS-BINDING`.

1.1.6 `Gate29=BLOCKED`; development freeze=`ACTIVE`; allowed effects=`Planning and Read-only observation only`.

## 1.2 Foundational critical path

1.2.1 critical node 1=`B0 authority`: v1 rejected with 22 findings; v2 successor generation in progress; Definition/Instance/receipt absent.

1.2.2 critical node 2=`Three-review Protocol`: v1.3 rejected with 24 findings; v1.4 candidate root generated; independent hostile review in progress; accepted 0/81.

1.2.3 critical node 3=`Source Universe and custody`: v3 rejected with 24 findings; v4 successor not yet generated; accepted SourceSet absent.

1.2.4 critical node 4=`TRD-2 review-closure requirements`: v2 rejected with 16 findings; v3 successor generation in progress; accepted Task Registry Definition absent.

1.2.5 critical node 5=`Master control sequence`: v1 rejected with 32 findings; v2 candidate has 30 Planning phases, 6 Post-Gate nodes and 79 edges; independent review absent.

1.2.6 critical node 6=`Public repository/cyber hardening`: predecessor rejected with 32 findings; v2 candidate has 52 Requirements and 161 edges; independent review absent; live settings credit zero.

1.2.7 join rule=`nodes 1–6 must each have an exact independently accepted current pointer before ReviewInputFreeze, Source admission, Task denominator or schedule receives credit`.

## 1.3 Ordered work after the foundational join

1.3.1 node 7=`freeze exact ReviewInput and accepted TRD-2 input roots; reconcile eligible reviews without Merge-by-presence`.

1.3.2 node 8=`discover and admit complete Program SourceSet, including PDF/text locators, Git overlay, Decisions, provider observations and superseded claims`.

1.3.3 node 9=`extract Requirement, Question, Decision, Conflict, Assumption, Scope, Package and Gate universes with forward/inverse coverage`.

1.3.4 node 10=`materialize Program Task Registry with atomic Tasks/Subtasks, owner-role requirements, dependency graph, safe terminals, acceptance predicates and Evidence requirements`.

1.3.5 node 11=`materialize resource/calendar assumptions, external waits, estimates, uncertainty bounds, critical path and scenario schedule from the accepted Task denominator`.

1.3.6 node 12=`assemble complete Master Plan human view and machine manifests, preserving every accepted source/decision/task identity`.

1.3.7 node 13=`run independent structural, semantic/security and schedule/estimation reviews; reconcile every finding under the accepted Protocol`.

1.3.8 node 14=`obtain exact-root Tal approval and publish an accepted Master current pointer under B0`.

1.3.9 node 15=`only after node 14, reassess Gate29 and request a new explicit instruction before any Product/Git/GitHub/provider execution`.

## 1.4 Why percentage and hours remain unavailable

1.4.1 candidate foundational nodes=`15`; this count is an ordering aid and not an accepted work denominator.

1.4.2 a node may generate one or more mandatory immutable successors after review; therefore `completed/15` would undercount newly discovered required Work.

1.4.3 Program Tasks do not exist until nodes 1–9 are accepted; Product completion numerator and denominator are both unavailable.

1.4.4 role capacity, named owners, external appointment calendars, provider response times, legal/finance review windows and accepted effort distributions are unavailable.

1.4.5 exact Product completion, exact Planning completion, remaining person-hours, calendar ETA and recent velocity=`unknown/unavailable`.

1.4.6 earliest lawful computation point=`after accepted Program Task Registry, resource calendar and estimate quality Gate at nodes 10–11`.

## 1.5 Safe current terminal

1.5.1 active independent work=`B0 v2 generation; Protocol v1.4 hostile review; TRD-2 v3 generation`.

1.5.2 queued next work=`B0 v2 hostile review; Source Universe v4 generation/review; TRD-2 v3 review; Control v2 review; Public/Cyber v2 review`.

1.5.3 prohibited shortcut=`treating a Producer QA PASS, crosswalk presence, URL observation, configured checkbox or candidate phase count as Acceptance or completion`.

1.5.4 no Product/Git/GitHub/provider action is authorized by this observation.
