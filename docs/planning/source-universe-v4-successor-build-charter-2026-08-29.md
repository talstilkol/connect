# 1. Connect — Source Universe v4 successor build charter

## 1.1 Identity and boundary

1.1.1 `charterId=CONNECT-SOURCE-UNIVERSE-V4-SUCCESSOR-BUILD-CHARTER-2026-08-29`.

1.1.2 predecessor Subject root=`sha256:6cb64b3877f194302a25fd25f5fa73c76a4d06d208f0e62a124e5591e5247092`.

1.1.3 predecessor independent-review root=`sha256:8c4bce0652c5a126f88449135370f2a4b1ef35dd582f1c518083a0911e08a7c9`.

1.1.4 predecessor Findings Manifest root=`sha256:e94273e22a07f498b40de34e2a5dc406b94b28bbef5e5ca9e2735b7302a14b1b`.

1.1.5 boundary=`builder instructions only; not a Source Universe Definition; not Acceptance; no Product/Git/GitHub/provider action`.

1.1.6 Public invariant=`repository remains Public; private source bytes and operational Evidence remain outside Public publication surfaces`.

## 1.2 Successor construction rules

1.2.1 create a new immutable v4 Subject, SourceOccurrenceLedger, TargetSpanLedger, typed producer/uses graph, conformance/mutation/generation manifests and detached Producer QA.

1.2.2 preserve every v3 Requirement identity and every v3 Finding identity without Merge, range credit, presence-only credit or closure transfer.

1.2.3 preserve all normative clauses from v2 and the 32 first-generation Findings through exact source-clause to successor-clause rows; `FULL` requires semantic comparison.

1.2.4 every successor Requirement has exactly five fields, one atomic output or closure-only assertion, backward-only typed dependencies and exact root+byte/span sourceBasis.

1.2.5 every test binds exact subject-row digest, dependency roots, admitted input/corpus root, operation bytes, evaluator profile, expected result bytes and one finite terminal.

1.2.6 no actual corpus, source byte, provider receipt or trusted time may be invented; absent inputs remain typed blocking inputs.

# 2. Ordered construction stages

## 2.1 Stage 1 — external foundations and schemas

2.1.1 define detached external B0, accepted Review Protocol, canonical mandate and Public policy roots before any local authority or Public projection.

2.1.2 define canonical serialization, deterministic content-derived IDs, byte-range/media-region algebra, terminal registry, relation types, state axes and authoritative-field registry.

2.1.3 close `SURS3-HR-F004,F010,F013,F017,F021,F022,F024` foundations before runtime/admission rows.

## 2.2 Stage 2 — literal occurrence and target-span identity

2.2.1 run two independent lexers over exact v4 bytes and emit one record for every literal source-token occurrence, including repeated token/consumer pairs.

2.2.2 assign occurrence ID, exact byte start/end, line/column observation, consumer field, token, target identity and inverse edge.

2.2.3 define 79-or-successor target spans with parser/schema root, heading/newline policy, exact extracted bytes and span digest.

2.2.4 close `SURS3-HR-F001,F002`; pair count and literal-occurrence count remain distinct denominators.

## 2.3 Stage 3 — object classes and sole producers

2.3.1 classify every object exactly once as external input, normative output, observation or Evidence.

2.3.2 assign one sole producer and separate SourceOccurrenceLedger, TargetSpanLedger and validation receipts.

2.3.3 generate the semantic uses graph from named uses and require identity with the explicit dependency graph under two extractors.

2.3.4 close `SURS3-HR-F003,F005,F006,F023`.

## 2.4 Stage 4 — custody before selection and admission

2.4.1 define candidate source family, byte custody, authenticity, authority, freshness, sufficiency, recursive safety, locator and axis profiles before selection.

2.4.2 define DiscoveryInputSet, complete frontier, inaccessible/withheld states and finite CandidateSet before denominator freeze.

2.4.3 require exact eligible independent admission-review receipt separate from selector/producer and prove non-collusion.

2.4.4 close `SURS3-HR-F005,F012,F016,F018,F019,F022`.

## 2.5 Stage 5 — Public classification before projection

2.5.1 accept classification/minimization/redaction/re-identification policy and secret/key/indirection lifecycle before any Public object or disclosure-safe Evidence construction.

2.5.2 define complete egress discovery from a separately frozen seed universe, accessible frontier and observation cutoff.

2.5.3 require detector-class corpus roots, per-class thresholds, zero critical-secret false negatives and UNKNOWN on inaccessible coverage.

2.5.4 close `SURS3-HR-F004,F014,F015,F016`.

## 2.6 Stage 6 — Decision, provider and dynamic-source authority

2.6.1 produce closed event-type/Appointment/field authority-effect matrix; research Events may attach Evidence but cannot mutate authoritative fields.

2.6.2 assign exact runtime-Evidence producer and acquisition edge for every ProviderReceipt.

2.6.3 bind dynamic observations to TLS/proxy/CDN/publisher-key/DNS/Vary/cookie/authenticated-cache/trusted-clock evidence and partition behavior.

2.6.4 close `SURS3-HR-F017,F018,F019`.

## 2.7 Stage 7 — archive, retention, Hold and erasure

2.7.1 separate archive, restore, retention, Legal Hold, erasure, tombstone, key custody and current-pointer outputs into atomic objects.

2.7.2 enumerate replica/cache/backup/export/escrow/wrapping-key paths and prevent restore from resurrecting erased current/readable objects.

2.7.3 enforce Public privacy for tombstone metadata and Hold precedence over deletion.

2.7.4 close `SURS3-HR-F006,F020,F023`.

## 2.8 Stage 8 — invalidation and successor publication

2.8.1 enumerate every mutable authoritative field across schemas, policies, algorithms, oracles and external lifecycle inputs.

2.8.2 map at least one trigger and inverse dependency edge to each field; union equality with AuthoritativeFieldRegistry is mandatory.

2.8.3 split trigger evaluation, invalidation event, affected Set, successor candidate and fenced current-pointer publication into separate atomic objects.

2.8.4 close `SURS3-HR-F006,F021,F023`.

## 2.9 Stage 9 — clause-lossless preservation

2.9.1 enumerate every normative v2 clause and all first-generation Finding fields at exact root+span.

2.9.2 map every source clause to one or more exact successor clauses and every successor preservation clause back to source.

2.9.3 independently classify `FULL/PARTIAL/ABSENT/CONTRADICTED/SUPERSEDED`; only `FULL` satisfies preservation.

2.9.4 explicitly verify toolchain binding, independent admission review, egress coverage, failure table, disagreement oracle, equations, relation cardinalities, closed triggers/publication and independence/tests.

2.9.5 close `SURS3-HR-F011,F012,F013,F014` and all preservation regressions.

## 2.10 Stage 10 — executable conformance and mutation

2.10.1 create one conformance envelope per successor Requirement with exact bytes and no prose evaluator.

2.10.2 create separately traceable negative vectors for every v2 Requirement, all 32 first-generation Findings and all 24 v3 Findings; no range or shared-presence credit.

2.10.3 bind canonical preimage, mutation operation, postimage, affected Set, unaffected Set, evaluator and expected terminal bytes.

2.10.4 require two clean-room evaluators/runners to emit byte-identical ledgers.

2.10.5 close `SURS3-HR-F007,F008,F010,F011,F013,F014`.

## 2.11 Stage 11 — controlled two-generation proof

2.11.1 materialize exact Generation A input/subject/output roots and Generation B input/subject/output roots.

2.11.2 freeze one canonical Delta, target locator, preimage, postimage, expected affected/unaffected Sets and stale-A terminal.

2.11.3 require both evaluators to reproduce A/B and reject every A receipt at B without recursive reliance on the Acceptance being proved.

2.11.4 close `SURS3-HR-F009`.

## 2.12 Stage 12 — review lifecycle and fenced acceptance

2.12.1 bind the externally accepted Review Protocol; define exact review slots, eligibility, quorum, findings, reconciliation choices, veto, appeal, expiry and revocation.

2.12.2 consume fresh external B0 authority and one-use Permit at the same atomic CAS that re-reads every dependency/current head.

2.12.3 publish exactly one detached current pointer after two independent readbacks; conflicts/reordering/concurrency/replay remain REVIEW-BLOCKED.

2.12.4 close `SURS3-HR-F024` and assert all 24 Finding closures only after exact Evidence.

# 3. Mandatory v3 Finding denominator

3.1 required distinct identities=`SURS3-HR-F001..F024`.

3.2 required severity preservation=`P0=12;P1=12;P2=0;P3=0` until independent reconciliation changes a reviewer-local disposition.

3.3 accepted closure before v4 independent review=`0/24`.

3.4 v4 Producer QA may prove mechanical form only; it cannot accept semantic closure.

3.5 Public repository visibility remains unchanged; Gate29 remains blocked.
