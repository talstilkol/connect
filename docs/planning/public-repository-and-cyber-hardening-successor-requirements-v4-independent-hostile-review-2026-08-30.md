# 1. Connect — Public Repository and Cyber Hardening v4 independent hostile review

## 1.1 Review identity and authority limit

1.1.1 `artifactId=CONNECT-PUBLIC-REPOSITORY-CYBER-HARDENING-V4-INDEPENDENT-HOSTILE-REVIEW-2026-08-30`.

1.1.2 `reviewId=PRCV4-IHR-2026-08-30`.

1.1.3 `artifactClass=DETACHED-INDEPENDENT-HOSTILE-REVIEW;PLANNING-ONLY;NOT-AUTHORITY;NOT-IMPLEMENTATION;NOT-OPERATIONAL-EVIDENCE;NOT-ACCEPTANCE;NOT-A-PERMIT`.

1.1.4 Frozen Subject=`docs/planning/public-repository-and-cyber-hardening-successor-requirements-v4-2026-08-30.md`;SHA-256=`0f1f5cc9fb349f999b0bbff3f6f683c47c951b793ce3ef847388530717ff7257`.

1.1.5 Frozen manifest envelope=`docs/planning/public-repository-and-cyber-hardening-successor-requirements-v4-atomic-package-manifest-2026-08-30.json`;SHA-256=`43bd110cd8b59c0a3ea6086203d804df7b0dc6dd3441ec443d7ec740c4e65ed5`.

1.1.6 Frozen declared package root=`f799c154c695034935c480a57b6a0047d8e2b67d318e42b0d9b88a0ea78f92cf`.

1.1.7 Mandatory late reconciliation input=`docs/planning/github-public-repository-large-generated-artifact-storage-decision-2026-08-30.md`;SHA-256=`508b702087bc2c4011975af87c30bea1208bf5720ec263409d287acb5eb15a84`.

1.1.8 Companion non-merged Findings Manifest=`docs/planning/public-repository-and-cyber-hardening-successor-requirements-v4-independent-hostile-review-findings-manifest-2026-08-30.md`.

1.1.9 This review did not use Producer QA or either stored Reader report as an authority. Their bytes and fresh executions were inspected only after independent derivation.

1.1.10 No Candidate, manifest, Producer QA, Reader, Reader report, predecessor, Product, Git, GitHub setting, provider, credential, deployment or release byte/state was changed.

## 1.2 Exact verdict

1.2.1 `byteAndRootDerivation=PASS`.

1.2.2 `declaredFiniteIdentityAndEdgeSets=PASS-WITH-ONE-INTERNAL-GRAPH-CONTRADICTION`.

1.2.3 `readerImplementedChecks=PASS/PASS;79/79 EACH`.

1.2.4 `independentMechanicalCoherence=FAIL` because a root-bound graph declares denominators that contradict its own arrays and both Readers accept that contradiction.

1.2.5 `semanticExecutability=FAIL`.

1.2.6 `lateStorageDecisionReconciliation=FAIL`.

1.2.7 `hostileReviewVerdict=REJECT-V4-AS-A-SEMANTICALLY-EXECUTABLE-SUCCESSOR;REQUIRE-IMMUTABLE-V5`.

1.2.8 New detached Findings=`23`;severity totals=`P0 22;P1 1;P2 0;P3 0`;OPEN=`23`;CLOSED=`0`;ACCEPTED=`0`;MERGED=`0`;SUPPRESSED=`0`.

1.2.9 The inherited 93-record closure universe remains separately OPEN and unaccepted: accepted closures=`0/93`. The 23 new Findings do not merge, replace, suppress or close any inherited record;the immutable-v5 active denominator is therefore `116` distinct Findings.

1.2.10 Review Acceptance=`0`;accepted Requirements=`0/42`;implemented Producers=`0/42`;operational Evidence roots=`0`;all four Permit classes=`ABSENT`.

# 2. Independent method

## 2.1 Byte, root and path method

2.1.1 Every one of the 15 physical inputs and seven package members was opened from the Git top-level discovered independently from the current directory.

2.1.2 Every structured locator was checked as a repository-relative `docs/planning/...` path, normalized without parent segments, backslashes, absolute prefixes or symlink escape. Every resolved input/member/report/QA locator was a regular non-symlink file inside the repository.

2.1.3 SHA-256, LF count and byte count were recomputed directly from physical bytes. No registry hash string was treated as proof of its own target.

2.1.4 The physical-input, semantic-core and atomic-package roots were recomputed from their declared domain prefixes and ordered path/NUL/hash/NUL/decimal-byte/LF tuples.

2.1.5 Subject, registry, graph, vector and closure semantics were parsed independently. Reader source was then inspected for what it actually checks, and both Readers were freshly executed.

## 2.2 Finite-universe method

2.2.1 The 42 Requirement IDs, output IDs, producer IDs, dependencies, failure terminals and planned states were compared one-to-one.

2.2.2 The graph was regenerated from the 25 input-consumption edges, 310 Requirement dependency edges, 42 production edges, 38 post-bootstrap authority edges and 204 Finding-closure requirement edges.

2.2.3 All 93 Finding identities were extracted from the three frozen source universes rather than inferred from v4 counters: 32 predecessor identities, 59 v2 wrapper identities and 34 v3-review identities.

2.2.4 Every v4 closure record, Alias record and vector was compared by exact ID, order, source family, severity, noMergeKey, mapped Requirement set and zero-credit state.

2.2.5 Permit schemas were compared field-by-field with the Subject's own TTL, CAS, replay, revocation, response-loss, recovery, role-separation and readback requirements.

## 2.3 Public-safety and late-input method

2.3.1 The live repository identity was read through the GitHub read-only repository API. The result was `visibility=public`, `private=false`, `archived=false`, `disabled=false`, `default_branch=main`, with a non-null platform repository node identity.

2.3.2 The live readback is a bounded review observation, not trusted-time Evidence and not Acceptance. It did not modify GitHub.

2.3.3 The Subject, seven package members, manifest, two reports and Producer QA were checked for common private-key, GitHub-token, AWS-key, JWT, email-address, absolute workstation-path and Windows-path patterns. No match was found. This bounded pattern check is not a semantic Secret/PII clearance.

2.3.4 Every package-member byte count was compared with the late decision's strict Regular Git threshold of `50 MiB=52428800 bytes`. Repository-size, growth and clone-time policy coverage was then inspected separately from present member size.

# 3. Independent results

## 3.1 Frozen bytes and roots

3.1.1 Subject SHA-256 matched the frozen value exactly.

3.1.2 Manifest-envelope SHA-256 matched the frozen value exactly.

3.1.3 All 15 physical inputs matched locator, SHA-256, LF count and byte count. Recomputed physical-input root=`0a509984df065b5a8bbf8777d74e0013bf5ab0801a7a2666faa3e3a347ade40a`.

3.1.4 All seven package members matched locator, SHA-256, LF count and byte count.

3.1.5 Recomputed semantic-core root=`d9a5932e7f2448e04e185a5baa59a7295a20d7e5ac6351f3d0fb485138cff2a1`.

3.1.6 Recomputed atomic-package root=`f799c154c695034935c480a57b6a0047d8e2b67d318e42b0d9b88a0ea78f92cf`.

3.1.7 Manifest self-exclusion and Reader-report exclusion prevent a direct self-hash cycle. That does not cure the missing acceptance binding described by `PRCV4-IHR-F017`.

## 3.2 Exact 93-Finding preservation

3.2.1 Source counts were independently extracted as predecessor=`32`,v2 wrapper=`59`,v3 new=`34`.

3.2.2 The v4 closure registry contains exactly 93 unique Finding IDs, 93 unique noMergeKeys and 93 unique vector IDs. Severity counts are exactly `P0=54`, `P1=38`, `P2=1`, `P3=0`.

3.2.3 Closure records 1–32 preserve the exact ordered predecessor IDs and wrapper IDs;records 33–59 preserve the remaining ordered v2 IDs;records 60–93 preserve the exact ordered v3 IDs.

3.2.4 All 32 Alias identity pairs match their two source lists, but every `fieldEquivalenceRoot` is null and every equivalence state is `UNPROVED`. Alias credit therefore correctly remains zero.

3.2.5 All 93 closure records have at least one mapped Requirement and one vector ID. Their mapped accepted roots, vector execution receipts, operational Evidence and independent dispositions are absent;all 93 accepted flags are false and all acceptance credit is zero.

3.2.6 ID/count preservation passes. Exact record-semantic preservation does not: the Readers never parse the source Finding fields and v4 supplies only whole-file source roots, not rooted record projections or a closed extraction grammar. See `PRCV4-IHR-F007`.

## 3.3 Requirements, sole Producers and planned states

3.3.1 Requirement IDs are contiguous `PRCV4-REQ-000..041`;output IDs, sole-producer IDs and planned-state IDs are each unique and one-to-one.

3.3.2 Dependency lists contain only prior Requirement IDs. Requirement 040 depends on exactly 000–039;Requirement 041 depends on exactly 000–040.

3.3.3 Counts are exactly Requirements=`42`,Producers=`42`,planned output states=`42`.

3.3.4 All Producer implementation, signer and capability roots are null;all Producer states are `ABSENT`;all Producer acceptance credit is zero.

3.3.5 All planned output heads, epochs and expiry values are null;all Evidence-root arrays are empty;all output acceptance credit is zero.

3.3.6 Producers 004–041 carry `bootstrapAuthorityObjectId=PRCV4-OBJECT-003`. Producers 000–003 carry no bootstrap authority. No admitted Genesis/appointment/recovery path authorizes those first four producers. See `PRCV4-IHR-F005`.

## 3.4 Graph

3.4.1 Physical arrays contain 109 unique nodes and 619 unique edges. All endpoints exist, every edge respects topological index, and independent traversal found a DAG.

3.4.2 Exact regenerated edge counts are input-consumption=`25`,object-consumption=`310`,production=`42`,authorization=`38`,Finding-closure=`204`;total=`619`. The regenerated edge set has zero missing and zero surplus edges.

3.4.3 Requirement 041 reaches all 42 Producers, all 42 output objects and all 25 input nodes transitively.

3.4.4 The same root-bound graph's `invariants` object declares `edges=631` and `closureRequirementEdges=216`. Those values contradict the physical arrays and manifest values `619/204`. See `PRCV4-IHR-F001`.

## 3.5 Schemas, canonical identity and lifecycle

3.5.1 The registry presents 42 `outputSchema` objects with 357 top-level required-field labels and `additionalProperties=false`.

3.5.2 Those labels reference 70 nested types that have no definition in the package, including authority, transition, reader, evaluator, Finding, Git, GitHub, scanner, witness, Permit-adjacent, incident and artifact record types.

3.5.3 Cross-field invariants are English strings. There are no materialized schema programs, evaluator bodies, positive corpus, malformed corpus or cross-field corpus roots.

3.5.4 PRCV4-REQ-008 promises the missing catalog, but its Producer and output are themselves absent. A promise to later define a schema is not the exact schema claimed by the current registry. See `PRCV4-IHR-F003`.

3.5.5 Package/core/input aggregate roots are domain-separated and recompute correctly. In contrast, individual file-member SHA values and the manifest-envelope SHA are raw SHA-256 over file bytes although Subject 2.4 says every digest uses an artifact-domain prefix. See `PRCV4-IHR-F004`.

3.5.6 Lifecycle transition content is represented only by an undefined `Transition` type plus prose/ID labels. The schema cannot execute expected-old CAS, one-use consumption, trusted-time rollback denial, response-loss recovery or descendant revocation. See `PRCV4-IHR-F006`.

## 3.6 Vectors and causal closure

3.6.1 Vector IDs and Finding IDs are unique and one-to-one across exactly 93 records. Every selected `fieldPath` is a real top-level required-field label of the chosen target Requirement, and every expected terminal text equals that Requirement's failure terminal.

3.6.2 Operation distribution is append=`53`,substitute identity=`18`,omit required property=`11`,stale CAS=`8`,reorder transaction=`1`,cross-use Permit=`1`,conflicting Evidence=`1`.

3.6.3 Every preimage is `SPECIFIED-NOT-MATERIALIZED`;every evaluator implementation root and execution receipt root is null;executed vectors=`0`;accepted vectors=`0`.

3.6.4 Operations contain no concrete preimage bytes, mutation operand or after-state. Seven of the eight stale-CAS vectors target a generic `State` or `Array<State>` field rather than a complete expected-old/current transition;the remaining one still has no current-head preimage or consume schedule. See `PRCV4-IHR-F008`.

3.6.5 There are 204 Finding-to-Requirement mappings. Fifty-eight Findings map to more than one Requirement, but every Finding receives only one vector against one target Requirement;compound closure predicates are not decomposed into causal mutations. See `PRCV4-IHR-F009`.

## 3.7 Reader executions and independence

3.7.1 Fresh Reader A command=`node docs/planning/public-repository-and-cyber-hardening-successor-requirements-v4-reader-a-2026-08-30.mjs`;exit=`0`;implemented verdict=`PASS`;checks=`79/79`.

3.7.2 Fresh Reader B command=`ruby docs/planning/public-repository-and-cyber-hardening-successor-requirements-v4-reader-b-2026-08-30.rb`;exit=`0`;implemented verdict=`PASS`;checks=`79/79`.

3.7.3 Fresh outputs reproduced stored report bytes exactly: Reader A report SHA-256=`1cd92bc70b270dab787d2761e659364ba6d66389a69b4a1a2fb393670e1e73af`;Reader B report SHA-256=`a578da069d5e98064450edb4d2d60a56e142fc4c023820b4bd3f6fbf220c7555`.

3.7.4 Reader A uses Node.js, custom recursive JSON lexical checks and Kahn traversal. Reader B uses Ruby, a duplicate-key Hash and depth-first traversal. Their language and DAG algorithms are distinct.

3.7.5 Their semantic check lists are near-isomorphic and hard-code the same counters/field lists. Neither checks the graph's declared invariant counters, nested type closure, evaluator bodies, source-record extraction, late storage decision or Permit transaction completeness. Both therefore return PASS over the same demonstrated contradiction. See `PRCV4-IHR-F002`.

## 3.8 Independent work, witnesses and no-self-acceptance

3.8.1 The dual allowlist schema stores only builder A, builder B and adjudicator Hashes. The graph has no distinct builder/adjudicator implementation or authority nodes, so one sole Producer can emit all three opaque roots. See `PRCV4-IHR-F010`.

3.8.2 The scanner schema uses an undefined `ScannerRecord` and receipt Hashes. The graph has no distinct scanner/adjudicator producer or authority nodes, so two-engine independence is not executable. See `PRCV4-IHR-F011`.

3.8.3 No current self-acceptance occurred: Requirement Acceptance=`0`,Review=`unimplemented`,all closure roots are absent and all Permits are absent.

3.8.4 Future no-self-acceptance is not proven. Producers 039 and 040 are authorized by the same bootstrap object;reviewer identities are undefined;the graph has no disjoint implementation/reviewer/acceptance-owner authority cut. See `PRCV4-IHR-F018`.

## 3.9 Four Permit classes

3.9.1 GitHub control-plane Permit state=`ABSENT`. Its schema lacks per-Permit issued/expiry time, expected-old Permit head/epoch, typed failure receipt and forward/response-loss recovery despite the Subject requiring them. See `PRCV4-IHR-F012`.

3.9.2 Public Push Permit state=`ABSENT`. Its schema has ref old/new OIDs and issued/expiry fields, but no exact accepted-object root distinct from sent objects, no typed failure receipt, no response-loss reconciliation and no Permit-head revocation/replay record. See `PRCV4-IHR-F013`.

3.9.3 Deployment Permit state=`ABSENT`. Its schema lacks independent reader identity, issued/expiry time, expected-old Permit/revision head, revocation record and response-loss/readback recovery. See `PRCV4-IHR-F014`.

3.9.4 Release Permit state=`ABSENT`. Its schema lacks independent reader identity, issued/expiry time, expected-old Permit/revision head, revocation record and authoritative post-publish failure/readback recovery. See `PRCV4-IHR-F015`.

3.9.5 Requirement 041 stores four generic State fields and three opaque separation Hashes. It supplies no closed Permit type tags, NamedUse/consumer signatures or cross-use evaluator. See `PRCV4-IHR-F016`.

## 3.10 Manifest, review and acceptance cut

3.10.1 Excluding the manifest envelope and Reader reports from the package root avoids a self-hash cycle.

3.10.2 Requirement 040 does not require the manifest-envelope SHA or Reader-report roots. A future acceptance can therefore bind the package member root while omitting the external envelope and the reports that claim to authenticate it. See `PRCV4-IHR-F017`.

3.10.3 Requirement 039 states `self review denied`, but that is a prose invariant over undefined ReviewerIdentity/Disposition types and a common bootstrap authority. It is not an executable separation predicate. See `PRCV4-IHR-F018`.

## 3.11 Public-safe locators, Secrets, PII and randomness

3.11.1 All live v4 structured artifact locators are repository-relative and use `docs/planning/...`;no `web/` prefix or absolute workstation path was found in the Candidate, envelope, reports or QA.

3.11.2 Current external readback confirms the repository remains PUBLIC. This observation does not create trusted-time, governance, Permit or Acceptance credit.

3.11.3 No common credential/token/private-key/JWT/email/absolute-path pattern was found in the live v4 package and companions. Six historical Secret coordinates remain unresolved, zero are cleared, and Public Push therefore remains blocked.

3.11.4 The frozen public Secret-scan observation publishes a deterministic commitment over a small coordinate set and describes equality between such commitments. That is an equality oracle inconsistent with v4's own public-projection rule, even though raw values are absent. See `PRCV4-IHR-F019`.

3.11.5 No prohibited randomness-based identity implementation occurs in the package or companions. IDs and ordering are deterministic, although many semantic identities remain unmaterialized.

## 3.12 Late large-artifact storage decision

3.12.1 The late decision hash matches `508b702087bc2c4011975af87c30bea1208bf5720ec263409d287acb5eb15a84`.

3.12.2 It is absent from v4's 15 physical inputs, graph, package root, Requirement dependencies and source-supersession state. An immutable v5 is mandatory;v4 must not be edited. See `PRCV4-IHR-F020`.

3.12.3 Present v4 package members total `545716` bytes;maximum member size=`126109` bytes;members at or above `52428800` bytes=`0`. The immediate member-size predicate passes for these seven members.

3.12.4 No v4 schema, Reader or vector enforces the strict member-size predicate, repository total-size budget, growth budget or clone-time budget. See `PRCV4-IHR-F021`.

3.12.5 No deterministic shard descriptor, ordinal/range continuity, reconstructed-corpus root, generator-first source contract, parity receipt or required missing/duplicate/reorder/overlap/gap mutation matrix exists. See `PRCV4-IHR-F022`.

3.12.6 No closed external Release/LFS/artifact-store contract binds public-safe classification, immutable identity, cost, retention, deletion, availability, provenance, expiry, owner and recovery behavior. See `PRCV4-IHR-F023`.

# 4. Finding cross-reference

## 4.1 Foundational package and authority defects

4.1.1 `PRCV4-IHR-F001` graph invariant denominator contradiction.

4.1.2 `PRCV4-IHR-F002` correlated Reader blind spot and false mechanical PASS.

4.1.3 `PRCV4-IHR-F003` unresolved nested schemas and absent evaluators.

4.1.4 `PRCV4-IHR-F004` raw file/envelope digests contradict universal domain separation.

4.1.5 `PRCV4-IHR-F005` no admitted Genesis/appointment/recovery for bootstrap producers.

4.1.6 `PRCV4-IHR-F006` lifecycle/CAS/time/revocation is not executable.

4.1.7 `PRCV4-IHR-F007` exact Finding extraction and Alias equivalence are unproved.

4.1.8 `PRCV4-IHR-F008` vectors have no materialized causal execution.

4.1.9 `PRCV4-IHR-F009` vector coverage collapses multi-Requirement closure predicates.

4.1.10 `PRCV4-IHR-F010` dual allowlist witnesses are not independently owned.

4.1.11 `PRCV4-IHR-F011` scanner witnesses and adjudicator are not independently owned.

## 4.2 Permit, acceptance and public-evidence defects

4.2.1 `PRCV4-IHR-F012` control-plane Permit transaction incomplete.

4.2.2 `PRCV4-IHR-F013` Public Push Permit transaction incomplete.

4.2.3 `PRCV4-IHR-F014` deployment Permit transaction incomplete.

4.2.4 `PRCV4-IHR-F015` release Permit transaction incomplete.

4.2.5 `PRCV4-IHR-F016` four-way Permit consumer/type separation is opaque.

4.2.6 `PRCV4-IHR-F017` external manifest/reports are not acceptance-bound.

4.2.7 `PRCV4-IHR-F018` no-self-review/no-self-acceptance authority cut is unproved.

4.2.8 `PRCV4-IHR-F019` public Secret-coordinate commitment creates an equality oracle.

## 4.3 Mandatory late-decision defects

4.3.1 `PRCV4-IHR-F020` late storage decision is outside the frozen v4 universe.

4.3.2 `PRCV4-IHR-F021` size/repository-growth/clone budgets are unenforced.

4.3.3 `PRCV4-IHR-F022` deterministic sharding and generator-first lifecycle are absent.

4.3.4 `PRCV4-IHR-F023` external artifact lifecycle is absent.

# 5. Final disposition

## 5.1 What passed

5.1.1 Frozen Subject and manifest hashes match.

5.1.2 All 15 physical inputs and seven package members match their byte records.

5.1.3 Physical-input, core and package roots recompute exactly.

5.1.4 Requirement/Producer/state identities are 42/42/42 and topologically ordered.

5.1.5 The physical 619-edge graph is an exact DAG with complete final reachability.

5.1.6 All 93 Finding IDs are preserved without merge, range or closure credit.

5.1.7 All live locators are repository-relative;no common Secret/PII token pattern, absolute path or randomness API was found.

5.1.8 The repository is currently observed PUBLIC.

## 5.2 What failed

5.2.1 The package is not mechanically self-consistent because its graph invariant counters contradict its own root-bound data.

5.2.2 It is not semantically executable because schemas, transitions, evaluators, witnesses, Permit transactions and no-self-acceptance predicates are not causally materialized.

5.2.3 Reader agreement cannot cure the defect because both implementations omit the same decisive checks.

5.2.4 The mandatory late storage decision is not in v4 and its size, growth, sharding and external-artifact lifecycle controls are absent.

## 5.3 Binding review state

5.3.1 `requiredSuccessor=IMMUTABLE-PUBLIC-CYBER-V5`.

5.3.2 v5 must preserve all predecessor/v4 bytes, import this Review and its 23-record Findings Manifest as frozen inputs, and close every record one-to-one without implicit credit.

5.3.3 `Acceptance=0`.

5.3.4 `GitHubControlPlanePermit=ABSENT;PublicPushPermit=ABSENT;DeploymentPermit=ABSENT;ReleasePermit=ABSENT`.

5.3.5 `repositoryVisibility=PUBLIC;Gate29=BLOCKED;developmentFreeze=ACTIVE`.
